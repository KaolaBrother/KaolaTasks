# Changelog

## Unreleased

- `#22` MCP 平时无仓库钥匙：提交进 git 的 `mcpServers` 示例只含 URL（不含 Agent Key、不含 forge PAT）。Agent Key 从环境变量 `KAOLA_AGENT_KEY` 注入用户级 `~/.cursor/mcp.json`。forge token 只在 REST claim `201` / MCP `claim_task` 成功时作为**该** `publicId` 的顶层 `token` 出现；再认领另一条任务拿到的是那条任务自己的 token。`claim.test.ts` / `mcp.test.ts` 钉了换任务换 token。README / DESIGN §7 / api.md 写明该用户模型。信封键未改（`task`、`token`、`lease`、`clone` 四键 `suggested_dir` / `token_usage` / `remote_url` / `extra_header`）。
- `#20` REST `POST /api/v1/tasks/:publicId/claim` `201` and MCP `claim_task` success share the same `clone` object with four keys: `suggested_dir`, `token_usage`, `remote_url`, `extra_header`. `remote_url` is stripped `repo.base_url` + `/` + `repo.full_name` + `.git` (no token in the URL). `extra_header` is `{ name: 'Authorization', value_pattern }` — gitea `token ${token}`, otherwise `Bearer ${token}` (literal `${token}`). Measured 2026-08-23 in worktree `.kw/worktrees/bundle-20-21`: `pnpm lint` / `pnpm typecheck` / `pnpm test` exit 0; node `--test` ℹ tests 545 / pass 545 / fail 0; vitest Test Files 5 passed, Tests 95 passed.
- `#21` publish pane: no `task-group-acceptance`. Imported source shows read-only `task-import-card` only after `POST /api/v1/tasks/import` `200`. `POST /api/v1/tasks` JSON is `{ title, description_md, source, repo, credential }` (still omit empty `base_branch` / `suggested_dir`); extra keys `acceptance_criteria` / `test_command` / `constraints` / `priority` / `tags` are omitted. Same measurement as `#20`.
- `#19` list open Issues from a credential profile (measured 2026-08-23 in worktree `.kw/worktrees/issue-19`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test` ℹ tests 540 / pass 540 / fail 0; vitest Test Files 5 passed, Tests 93 passed; web vite v7.3.6, 2568 modules, `dist/assets/index-tiuGMZIt.js` 1,486.88 kB │ gzip: 412.09 kB, chunk-size warning only): `@kaola/forge-adapters` adds adapter method `listIssues(cred, repo)` returning `ListedIssue[]` `{ number, title, issue_url }`. Fetch origin matches `importIssue`/`getPullRequest` (GitHub always `https://api.github.com`; GitLab/Gitea constructor `baseUrl`). Query strings: GitHub `state=open&per_page=50&sort=created&direction=desc`; GitLab `state=opened&per_page=50&order_by=created_at&sort=desc`; Gitea `state=open&type=issues&limit=50`. `issue_url` is built from `repo.base_url` (GitHub/Gitea `…/issues/{number}`, GitLab `…/-/issues/{iid}`); GitHub drops items with a `pull_request` key. New shared spec `packages/forge-adapters/src/list-issues.shared.test.ts`. `@kaola/server` `GET /api/v1/credential-profiles/:id/issues` (session `active`+`full`) decrypts the profile token server-side and calls `listIssues`; `200` `{ issues: [{ number, title, issue_url }] }`; `404` `{ error: 'not_found' }`; `500` `{ error: 'vault_unconfigured' }`; forge 401 → `422` `{ error: 'token_check_failed', missing: ['读'], message: 'token 无效或无权读取该 Issue。' }` (same message as import); other forge failures including 404/410 → `502` `{ error: 'forge_unreachable', message: '无法连接 forge 列出 Issue。' }` (not `issue_not_found`). Does not write `token 揭示` and is not a third token-reveal channel. New test `apps/server/src/credential-profile-issues.test.ts`. `@kaola/web` publish form: profile path hides Forge / 仓库地址 / 仓库 and, when source is imported, selects Issue from a dropdown (`#{number} {title}`); empty profiles show `暂无凭证档案，请先到钥匙页添加。`; inline token still pastes Issue URL and still fills repo fields.
- `@kaola/server` OAuth start paths now send a real authorize `scope` (`read:user` / `read_user` / `read:user`). Omitting it made `@fastify/oauth2` put `scope=undefined` on the GitLab URL, which GitLab.com rejects.
- Root `README.md` is now a user guide (login, publish, Agent/MCP, local run, Docker). Issue-by-issue implementation notes stay in this changelog and `docs/api.md`.
- `@kaola/web` (#18 Eucalyptus Ink workbench shell): Naive `theme-overrides` from `apps/web/src/theme.ts` (Eucalyptus Ink tokens Paper `#F3F6F4` / Ink `#1C2420` / Leaf `#3D6B54` / Bark `#6B746F` / Slip `#FFFEFB` / Clay `#B4532A`) plus `theme.css` (`@media (max-width: 768px)` stacks nav). Member workbench is a four-pane shell (`workbenchPane` `'board' | 'publish' | 'keys' | 'audit'`, nav 看板/发布/钥匙/审计, panes via `v-show`, no vue-router). `claim_only` omits 发布 nav/pane and 凭证档案 (`canApprove` = `active`+`full`). Publish form groups (`task-group-task` / `repo` / `acceptance` / `credential`) plus `<details>` 高级 (`task-group-advanced`, closed by default). Credential `base_url` prefills github → `https://github.com`, gitlab → `https://gitlab.com`, gitea stays empty; a non-empty typed value is not overwritten on forge switch. Poster `full` detail buttons 取消 / 重新开放 call the existing `PATCH /api/v1/tasks/:publicId` (`{ status: '已取消' }` / `{ status: '待认领' }`); no new HTTP routes. Still no Agent claim UI. Forge token reveal remains REST claim `201` top-level `token` and MCP `claim_task` success `token`. New test file `apps/web/src/App.shell.test.ts` (11 `it(` call sites); vitest `include` already `src/**/*.test.ts`, so root `pnpm test` picks it up via `pnpm --filter @kaola/web test`.
- `@kaola/server` (#16 autonomous claim confirmation gate + trusted automation): new `apps/server/src/claim-confirmations.ts` exports `registerClaimConfirmations(app, db)` (wired into `buildApp` after `registerClaim`, before `registerEvents`) plus helpers `findClaimConfirmations`, `insertPendingConfirmation`, `consumeApprovedConfirmation`, `recordPendingConfirmEvent`, `pendingConfirmationBody` consumed by `claim.ts`. `claimTask(db, auth, publicId, autonomous?: boolean)` (`apps/server/src/claim.ts`) gained a fourth parameter; REST `POST /api/v1/tasks/:publicId/claim` reads it from an optional JSON body `{ autonomous?: boolean }` (missing body / non-object / non-boolean value → `undefined`, i.e. instructed — unchanged for every existing caller that sends no body); MCP `claim_task` gained matching optional input `autonomous: z.boolean().optional()`, forwarded straight into `claimTask`. Instructed claims (`autonomous` omitted or `false`) are byte-for-byte unchanged — still 认领即授权, still `201` with top-level `token` on success. Only `autonomous: true` from a claimer whose `users.trusted_automation` (new column, default `false`) is not `true` engages the gate, evaluated after the existing 待批准 `403` and the existing lease/state `409`s, keyed on the `(task, user, agent_key)` triple via `findClaimConfirmations`: an existing `pending` row for that triple short-circuits straight to `202` again (no duplicate insert, no duplicate event); an existing `approved` row is consumed (the row is deleted, not flipped to another state, so a later release + re-claim can never ride the same approval a second time) and the claim then proceeds through the normal `201` token-reveal path; otherwise a new `pending` row is inserted, a `认领待确认` event is recorded (`actorUserId` = the claimer), and the response is `202` `{ error: 'confirmation_required', message: CONFIRMATION_REQUIRED_MESSAGE, pending: true }` — no token, no lease insert, task status stays `待认领`. `mcp.ts`'s existing `toToolResult` treats this `202` as `ok: true` (a service-level success, not `isError`), so a pending MCP `claim_task` call still returns non-error `structuredContent` — which has no `token` key. New session endpoints, gated by a new `requireActiveSessionUser` helper (401 for no session or a `待批准` session, same oracle as `PUT /api/v1/me/settings`): `GET /api/v1/claim-confirmations` → `200` `{ confirmations: [{ id, task_id, state, created_at }] }`, only the caller's own rows (`state` one of `'pending' | 'approved' | 'rejected'`, `task_id` is the public id via an inner join); `POST /api/v1/claim-confirmations/:id/approve` → sets `state: 'approved'`, writes a `认领已确认` event, `200 { ok: true }`; `POST …/:id/reject` → sets `state: 'rejected'` directly (no intermediate read), `200 { ok: true }`, no event; both 404 `{ error: 'not_found' }` for a non-positive-integer, non-existent, or not-owned id. Neither approve nor reject touches `tasks` or `leases`, and neither ever reveals a token — approving only lets the *next* matching autonomous claim attempt consume it. `apps/server/src/auth.ts`: `GET /api/v1/me` response gained additive key `trusted_automation: boolean`. New `PUT /api/v1/me/settings` (same 401 gate as above) body `{ trusted_automation: boolean }` (missing/non-boolean → `400 { error: 'invalid_body' }`) → `200 { trusted_automation }`, persisted via `UPDATE users SET trusted_automation = ...`. `apps/server/src/schema.ts` / `db.ts`: `users` gained column `trustedAutomation` / `trusted_automation` (`INTEGER NOT NULL DEFAULT 0`, drizzle boolean mode); a sqlite file created before this issue gets the column via a best-effort `ALTER TABLE users ADD COLUMN trusted_automation INTEGER NOT NULL DEFAULT 0` that swallows a "duplicate column name" error (`isDuplicateColumnError`), so re-running `createDb` against an already-migrated file is a no-op. New table `claim_confirmations` (`id INTEGER PRIMARY KEY AUTOINCREMENT`, `task_id`, `user_id`, `agent_key_id` all `INTEGER NOT NULL`, `state TEXT NOT NULL`, `created_at INTEGER NOT NULL`) via `CREATE TABLE IF NOT EXISTS`. Forge token reveal remains exactly the same two channels: REST claim `201` top-level `token` and MCP `claim_task` success (non-pending) `token`; `/claim-confirmations`, `/me`, `/me/settings`, and every `202`/pending response never contain one. New test file `apps/server/src/claim-confirm.test.ts` (14 `test(` call sites).
- `@kaola/web` (#16 settings + confirmation UI): the existing `Agent Key` block's visibility gate (`canManageKeys` = `me.value?.status === 'active'`, so both `full` and `claim_only` members) now also covers two new sections rendered directly above it. 受信自动化: `n-switch` `data-testid="trusted-automation-toggle"` bound to `trustedAutomation`, seeded from `GET /api/v1/me`'s `trusted_automation` on mount and updated via `PUT /api/v1/me/settings` on toggle (`{ trusted_automation: value }`; on success `trustedAutomation` is set from the response body, not just echoed). 待确认认领: `data-testid="claim-confirmation-list"`, populated from `GET /api/v1/claim-confirmations` (loaded whenever `canManageKeys` is true); each row shows `#{id} 任务 {task_id} · {created_at}` plus `data-testid="claim-confirmation-approve"` / `"claim-confirmation-reject"` buttons that `POST` to the matching endpoint and reload the list; empty list shows `暂无待确认认领。`. No Agent-facing claim UI is added — Agents still claim exclusively via Bearer/MCP; this list is strictly for a human to approve or reject an Agent's autonomous claim attempt. Still no vue-router. New test file `apps/web/src/App.settings.test.ts` (8 `it(` call sites).
- `@kaola/server` (#15 audit log + team stats HTTP): new `apps/server/src/events.ts` exports `registerEvents(app, db)`, wired into `buildApp` after `registerClaimConfirmations` (before `registerMcp`). `GET /api/v1/events`: session-only; a shared `canReadEvents` check requires `user.status !== '待批准'` — stricter than `GET /api/v1/tasks`, which lets a `待批准` session read the board read-only. No session or a `待批准` session → `401 { error: 'unauthorized' }` via the existing `sendUnauthorized`; any other logged-in status (`active` or `claim_only`) → `200 { events: [...] }`. Rows come from `events` left-joined against `users` on `actor_user_id`, ordered `desc(events.id)` (newest id first); each row is `{ id, type, actor_user_id, actor_username, created_at, details }` — `created_at` is the stored unix-seconds column rendered as ISO-8601; `details` is the stored TEXT column `JSON.parse`d into an object (`{}` on parse failure or when the parsed value isn't a plain object); `actor_username` is `null` whenever `actor_user_id` is `null` (e.g. a system-driven `状态迁移`). There is no query string on this route — every filter (type / actor / task / time range) in the UI is client-side over the full returned set. `GET /api/v1/stats`: identical auth gate. `200 { completed_count, completed_by_username }`, computed only from `events` rows where `type === '状态迁移'` and the parsed `details.to === '已完成'`; `completed_count` is that row count; `completed_by_username` groups the same rows by `actor_username`, with a `null` actor (or an actor id that fails to resolve to a username) bucketed under the literal string `系统`. Neither endpoint ever includes a forge token; token reveal is still exactly REST claim `201` top-level `token` and MCP `claim_task` success `token`. New test file `apps/server/src/events.test.ts` (9 `test(` call sites).
- `@kaola/web` (#15 audit UI): member workbench gains two new sections, rendered between the existing board detail and 批准 GitHub 用户, visible to any non-`待批准` session (both `full` and `claim_only`, since the whole block sits inside the existing `view === 'member'` branch — no new gate). 审计日志 (`data-testid="audit-section"`): client-side filters 类型/人/任务/时间 (`data-testid` `audit-filter-type` / `audit-filter-actor` / `audit-filter-task` / `audit-filter-from` / `audit-filter-to`) over the full list from `GET /api/v1/events` (loaded once on mount, alongside `loadTasks`/`loadStats`, when `view.value === 'member'`); each row (`data-testid="audit-row"`) renders `#{id} {type} · {actor_username ?? 系统} · {created_at} · {details as JSON text}`; empty result shows `暂无审计记录。`. 团队统计 (`data-testid="stats-section"`): `完成数：{completed_count}` (`data-testid="stats-completed-count"`) plus one line per `completed_by_username` entry (`{username}：{count}`), from `GET /api/v1/stats`; empty shows `暂无完成记录。`. Still no vue-router; still no Agent claim UI (unrelated to #16, unchanged here). New test file `apps/web/src/App.audit.test.ts` (16 `it(` call sites).
- Root `pnpm test` file list: now also runs `apps/server/src/events.test.ts` and `apps/server/src/claim-confirm.test.ts`, both appended after `apps/server/src/writeback.test.ts` and before `pnpm --filter @kaola/web test` — exact script string is `node --experimental-strip-types --test packages/shared/src/index.test.ts packages/forge-adapters/src/index.test.ts packages/forge-adapters/src/validate-token.shared.test.ts packages/forge-adapters/src/get-pull-request.shared.test.ts packages/forge-adapters/src/import-issue.shared.test.ts packages/forge-adapters/src/webhook.shared.test.ts packages/forge-adapters/src/comment-on-issue.shared.test.ts apps/server/src/import.test.ts apps/server/src/placeholder.test.ts apps/server/src/auth.test.ts apps/server/src/agent-keys.test.ts apps/server/src/vault.test.ts apps/server/src/tasks.test.ts apps/server/src/hosting.test.ts apps/server/src/claim.test.ts apps/server/src/mcp.test.ts apps/server/src/poller.test.ts apps/server/src/webhook.test.ts apps/server/src/writeback.test.ts apps/server/src/events.test.ts apps/server/src/claim-confirm.test.ts && pnpm --filter @kaola/web test`. Measured 2026-08-22 (`CI=true`) in worktree `.kw/worktrees/bundle-15-16`: `pnpm lint` / `pnpm typecheck` / `pnpm build` exit 0; node `--test`: ℹ tests 502  ℹ suites 110  ℹ pass 502  ℹ fail 0; vitest: Test Files 4 passed (4); Tests 75 passed (75); web vite v7.3.6, 2565 modules, `dist/assets/index-UEKvJUM2.js` 1,472.36 kB │ gzip: 407.73 kB (chunk-size warning only, unchanged from #14 since the chunking strategy itself was not touched).
- `@kaola/forge-adapters` (#14 status write-back): `IssueRef` is no longer `unknown` — `export type IssueRef = { issue_url: string }`. `commentOnIssue(cred, issueRef, body)` is implemented on all three adapters (was `notImplemented`; that helper is now unused by this method). Reuses the existing `resolveImportedIssue` (same URL parsing + host/SSRF rule as `importIssue`/`getPullRequest`: GitHub always `https://api.github.com`, GitLab/Gitea use the constructor `baseUrl`, never the pasted `issue_url` host) and the existing `forgePost`. POST path: GitHub/Gitea `${issueApiUrl}/comments`; GitLab `${issueApiUrl}/notes`. JSON body `{ body }` for all three. Success is any `res.ok` (2xx), not a hard-coded 201. Non-OK response rejects after the one fetch with `commentOnIssue: ${kind} responded ${status}`. An unparseable `issue_url` (including a pasted PR/MR web path) rejects without calling `fetch`. Auth headers reuse the existing per-kind `authHeaders()` (GitHub `Bearer`, GitLab `PRIVATE-TOKEN`, Gitea `token`). New shared spec `packages/forge-adapters/src/comment-on-issue.shared.test.ts`, parameterized over github/gitlab/gitea (19 `it(` call sites: 5 per kind × 3 plus 4 standalone), copied/trimmed fetch-stub helpers (not imported from `import-issue.shared.test.ts`).
- `@kaola/server` (#14 status write-back): new `apps/server/src/writeback.ts` exports `attemptWriteback(db, task, transition: '认领' | '提交PR' | '完成', actorUserId: number | null, prUrl?): Promise<void>` — posts a `commentOnIssue` status comment on the task's source Issue for **imported** tasks only (`task.sourceType === 'imported'` and non-empty `task.sourceIssueUrl`; native tasks are a no-op, no forge call, no event). Hooked after the status transition is already committed (never inside a `db.transaction`, never holding a SQLite write lock across the outbound HTTP call): `claimTask` → `认领` (`actorUserId` = the claiming user); `submitPr` → `提交PR` (same); `applyPrTerminalTransition` → `完成` **only when `terminal === 'merged'`** (`actorUserId: null`) — never on `terminal === 'closed'` (已退回) and never on `releaseTask`. `claimTask` and `submitPr` (`apps/server/src/claim.ts`) are now `async`; REST `registerClaim` and MCP `claim_task`/`submit_pr` (`mcp.ts`) `await` them. No new/changed HTTP routes or response shapes — claim `201` still returns `token`, `submit_pr` still returns `待验收`, completion still reaches `已完成`, all unaffected by a write-back failure (every fault — forge throw, decrypt miss, unparseable `issue_url` — is caught and swallowed inside `attemptWriteback`). Success writes `events.type` `回写`, `details` **exactly** `{ task_id, transition, ok: true, issue_url }` (`task_id` is the public id; no token, no ciphertext). Comment body always contains `task.publicId` and `PUBLIC_URL` (trailing slash trimmed, default `http://localhost:31415`, same convention as `auth.ts`); the 提交PR and 完成 bodies additionally include the submitted/merged `pr_url`. There is still no `/tasks/:id` deep link in `@kaola/web` (no vue-router), so the comment only carries `PUBLIC_URL` plus the publicId, not a task URL. Credential resolution is `decryptTaskToken(db, task)` — moved from `poller.ts` into `writeback.ts` (the poller still imports and uses it for its own `getPullRequest` call) — the task's own profile/inline credential, never the caller's Agent API key. `retryPendingWritebacks(db: AppDb): Promise<void>` is exported from `writeback.ts` and re-exported from `poller.ts` (never rejects; a DB fault or one task's fault only skips that task); it re-attempts any imported task whose transition has occurred (a `状态迁移` to `进行中` for 认领; a `submissions` row for 提交PR; `status === '已完成'` for 完成) but has no successful `回写` event yet (`details.ok === true` for that `task_id` + `transition`) — a transition with a successful `回写` is never retried again. `apps/server/src/app.ts`'s existing poller timer now also calls `retryPendingWritebacks(db)` every tick, sequentially right after `pollPendingReviews`, under the same in-flight guard. On a **merged** webhook delivery (`webhook.ts`'s `applyPrTerminalTransition` call, `poller.ts`), write-back now decrypts the task's forge credential to post the 完成 comment — this is new: the webhook path previously never decrypted anything. The merge/close verdict itself is still decided from the signed payload alone (`parseWebhook`); the webhook route still never calls `getPullRequest`. `closed` (已退回) deliveries still never write back and still never decrypt. Forge token reveal remains exactly the same two channels: REST claim `201` top-level `token` and MCP `claim_task` success `token`; write-back (including the webhook-merged decrypt) never puts a token in a response, a log, or `events.details`. New test file `apps/server/src/writeback.test.ts` (15 `test(` cases: drives real 认领/提交PR/完成 through the REST/MCP/webhook/poller surface with a mocked `globalThis.fetch`, asserting exactly one comment POST per transition, task-credential auth headers (not the Agent key), native-task no-op, forge-5xx/network-failure non-blocking, `已退回` never comments, and `retryPendingWritebacks` re-posting a failed attempt without duplicating a succeeded one). Root `package.json` `test` script: `packages/forge-adapters/src/comment-on-issue.shared.test.ts` added alongside the other `*.shared.test.ts` files; `apps/server/src/writeback.test.ts` added after `webhook.test.ts`. Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-14`: node `--test`: ℹ tests 479  ℹ suites 99  ℹ pass 479  ℹ fail 0; vitest: Test Files 2 passed; Tests 51 passed (51).
- `@kaola/forge-adapters` (#13 webhook receivers + registerWebhook): `ForgeEvent` is no longer `unknown` — `{ type: 'pull_request'; state: 'merged' | 'closed'; pr_url: string; repo: { full_name: string } }`. New `WebhookSignatureError extends Error` (`name === 'WebhookSignatureError'`), thrown by `parseWebhook` on a bad/missing signature or secret; `parseWebhook` returns `null` (never throws) for a signature-valid-but-irrelevant delivery (ping, non-`pull_request`/non-`Merge Request Hook`, non-`closed` action, unparseable JSON, missing `pr_url`/`repo.full_name`). `CreateForgeAdapterOptions` gained `webhookSecret?: string`. `registerWebhook(cred, repo, callback)` and `parseWebhook(headers: Headers, body: unknown)` are implemented on all three adapters (were `notImplemented`); `commentOnIssue` still throws `Error('not implemented')` (#14, out of scope). `parseWebhook`: missing/empty `webhookSecret` throws `WebhookSignatureError` before any header check; verifies against the **raw** body string/Buffer (never a re-serialized JSON object), then `JSON.parse`s (parse failure → `null`, not a throw). Per-forge signature check: GitHub `X-Hub-Signature-256` header, expected `sha256=${createHmac('sha256', secret).update(rawBody).digest('hex')}`, compared via a length-checked `timingSafeEqual` wrapper (`timingSafeEqualStrings`); Gitea `X-Gitea-Signature` header, same HMAC digest **without** the `sha256=` prefix; GitLab `X-Gitlab-Token` header, plain `timingSafeEqualStrings` against `webhookSecret` (legacy secret-token scheme, not GitLab's newer `webhook-signature`/`signing_token` HMAC — explicitly out of scope per the ruling). Event mapping: GitHub/Gitea (`mapGithubShapedEvent`) require header `x-github-event`/`x-gitea-event === 'pull_request'` and payload `action === 'closed'`; `state` is `'merged'` when `pull_request.merged === true` else `'closed'`; `pr_url` = `pull_request.html_url`; `repo.full_name` = `repository.full_name`. GitLab (`mapGitlabEvent`) requires header `x-gitlab-event === 'Merge Request Hook'` and `object_attributes.state` in `('merged', 'closed')`; `pr_url` = `object_attributes.url`; `repo.full_name` = `project.path_with_namespace`. `registerWebhook`: GitHub `POST https://api.github.com/repos/{owner}/{repo}/hooks` body `{ name: 'web', events: ['pull_request'], config: { url: callback, content_type: 'json', secret, insecure_ssl: '0' } }`; GitLab `POST {baseUrl}/api/v4/projects/{encodeURIComponent(repo.full_name)}/hooks` body `{ url: callback, merge_requests_events: true, token: secret }` (legacy `token`, not `signing_token`); Gitea `POST {baseUrl}/api/v1/repos/{owner}/{repo}/hooks` body `{ type: 'gitea', events: ['pull_request'], config: { url: callback, content_type: 'json', secret }, active: true }` (owner/repo each individually `encodeURIComponent`-ed, same as the GitHub branch — a security-review fix, R2, non-blocking). All three reject after a non-OK response with `registerWebhook: ${kind} responded ${status}`; auth headers reuse the existing per-kind `authHeaders()`. GitHub/GitLab/Gitea host rules for `registerWebhook` are unchanged from `getPullRequest`/`importIssue` (GitHub always `https://api.github.com`; GitLab/Gitea use constructor `baseUrl`); `parseWebhook` never fetches. New shared spec `packages/forge-adapters/src/webhook.shared.test.ts` parameterized over github/gitlab/gitea (18 top-level `it(`/`describe(` call sites over 4 `describe` blocks, copied/trimmed fetch-stub helpers from `get-pull-request.shared.test.ts`, not imported from it; one `it()` block runs once per forge kind).
- `@kaola/server` (#13 webhook receivers + configurable polling fallback): new `apps/server/src/webhook.ts` exports `registerWebhooks(app, db, forgeInstances?)`, wired into `buildApp` as `registerWebhooks(app, db, forgeInstances)` after `registerMcp`. `POST /api/v1/webhooks/:publicId` — no session, no Bearer; the forge signature is the sole auth. `:publicId` identifies a `forgeInstances[]` entry (not a task). A dedicated `addContentTypeParser('application/json', { parseAs: 'string' }, ...)` is registered only inside this route's own `app.register(async function webhookContext(child) {...})` child plugin, so the raw body string survives for HMAC verification without touching any other route's JSON parsing. Unknown `publicId` → `404` `{ error: 'not_found' }`. `WebhookSignatureError` from `adapter.parseWebhook` → `401` `{ error: 'invalid_signature' }` (any other thrown error rethrows/500s). `parseWebhook` returning `null` → `204` empty body. On a concrete `ForgeEvent`: `findPendingReviewMatch(db, instance, event.pr_url)` scans `tasks` where `status === '待验收'`, keeps only rows where `taskMatchesForgeInstance(task, instance)` (exact `(task.repoForge, task.repoBaseUrl) === (instance.forge, instance.baseUrl)`) — binding the match to the signature-verified instance before ever comparing `pr_url`, closing a cross-instance confused-deputy gap found in review (see R1 below) — then matches the task's latest `submissions.prUrl` via the existing `latestSubmission`. No match (wrong instance, wrong `pr_url`, or task no longer `待验收`) → `204`, no writes. On match: `applyPrTerminalTransition(db, task, submissionId, event.state, event.pr_url)` (new export from `apps/server/src/poller.ts`, extracted from the poller's own transaction — same `db.transaction` shape: `tasks.status` → `已完成`/`已退回` via `transitionTaskStatus`, `submissions.prState` → `'merged'`/`'closed'`, one `insertAuditEvent` `状态迁移` with `actorUserId: null` and `details: { task_id: task.publicId, from, to, pr_url }`) then `204`. Never decrypts a forge token and never calls `getPullRequest` on this path — the payload is the sole source of merge/close truth. `apps/server/src/poller.ts` gained exported type `ForgeInstanceConfig = { publicId: string; forge: ForgeKind; baseUrl: string; syncMode: 'webhook' | 'poll'; webhookSecret: string }` and exported `taskMatchesForgeInstance(task, instance: Pick<ForgeInstanceConfig, 'forge' | 'baseUrl'>): boolean`; `pollPendingReviews(db, forgeInstances?)` gained the optional second parameter and now skips (via `isWebhookManaged`, `some(i => i.syncMode === 'webhook' && taskMatchesForgeInstance(task, i))`) any `待验收` task whose `(repoForge, repoBaseUrl)` matches a `syncMode: 'webhook'` instance — zero `getPullRequest` calls for that task; omitted/`undefined`/`[]` forgeInstances polls every `待验收` row exactly as before this issue; a `syncMode: 'poll'` instance (or no matching instance) is still polled. `buildApp` gained `forgeInstances?: ForgeInstanceConfig[]` option, threaded into both the poller-timer closure (`pollPendingReviews(db, forgeInstances)`) and `registerWebhooks(app, db, forgeInstances)`. `apps/server/src/index.ts` gained `readForgeInstances()`: parses `process.env.FORGE_INSTANCES` as a JSON array; unset or `''` → `[]`; non-array or invalid JSON throws (fails boot; does not silently fall back to poll-everything). No new database table — `forgeInstances` is process config, not persisted state, per the orchestrator ruling. New test file `apps/server/src/webhook.test.ts` (11 `test(` call sites, drives a task through the real HTTP+MCP surface to `待验收` then POSTs signed deliveries at `app.inject` against a real `buildApp({ forgeInstances })`) plus 4 new `test(` cases appended to the pre-existing `apps/server/src/poller.test.ts` (webhook-mode skip, poll-mode still-polled, empty-array-same-as-omitted, and a pre-existing resilience case) — no existing `poller.test.ts` case was weakened. A security review (`sec-review.md`) found one blocking issue (R1: a signed delivery on instance A could complete any `待验收` task by attacker-chosen `pr_url`, because the original `findPendingReviewMatch` matched on `pr_url` alone across every instance) and one non-blocking issue (R2: Gitea's `registerWebhook` interpolated `repo.full_name` unencoded into the hook-creation path). Both are fixed in the implementation described above and confirmed closed by a re-review (`sec-rereview.md`, verdict pass, 0 blocking findings). Root `pnpm test` now includes `packages/forge-adapters/src/webhook.shared.test.ts` (after `import-issue.shared.test.ts`) and `apps/server/src/webhook.test.ts` (after `poller.test.ts`). Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-13`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test`: ℹ tests 445  ℹ suites 89  ℹ pass 445  ℹ fail 0; vitest: Test Files 2 passed; Tests 51 passed (51); web vite v7.3.6, 2565 modules, `dist/assets/index-MeXOcpNK.js` 1,465.50 kB │ gzip: 406.21 kB (chunk-size warning only, unchanged from #12 since `@kaola/web` was not touched by #13).
- `@kaola/forge-adapters` (#12 Issue import): `ImportedIssue` is no longer `unknown` — `{ title: string; description_md: string; issue_url: string; repo: { full_name: string } }`. `importIssue(cred, issueUrl)` is implemented (was `notImplemented`) on all three adapters. Parses owner/repo/number (GitHub and Gitea `.../issues/{n}`) or namespace/iid (GitLab canonical `.../-/issues/{iid}`, then legacy `.../issues/{iid}`) out of the pasted Issue web URL; a trailing slash is stripped first (`replace(/\/+$/u, '')`); query and hash are dropped by `URL.pathname`. Builds the REST endpoint via the same `prApiOrigin` as `getPullRequest`: GitHub always `https://api.github.com/repos/{owner}/{repo}/issues/{number}` (ignores constructor `baseUrl` and the pasted host); GitLab `{baseUrl}/api/v4/projects/{encodeURIComponent(namespace)}/issues/{iid}`; Gitea `{baseUrl}/api/v1/repos/{owner}/{repo}/issues/{number}` — GitLab/Gitea use the adapter's constructor `baseUrl`, never the issueUrl's own host. Auth headers reuse `forgeGet` / `authHeaders`. `description_md` comes from GitLab JSON `description` and from GitHub/Gitea JSON `body` (`null`/missing/non-string → `''`). Returned `issue_url` is the pasted web URL after trailing-slash strip (not API `html_url`/`web_url`). Missing/non-string JSON `title` rejects after the one fetch. Non-OK HTTP rejects after fetch with `importIssue: ${kind} responded ${status}`. Unparseable URLs (including `/pull/`, `/pulls/`, `/-/merge_requests/`, `/-/work_items/`) reject without calling `fetch`. Package-level export `parseIssueUrl(kind, issueUrl): { full_name: string } | undefined` (same parsers; not an adapter method). `registerWebhook` / `parseWebhook` / `commentOnIssue` still throw `Error('not implemented')`. `ForgeEvent` / `IssueRef` stay `unknown`. New shared spec `packages/forge-adapters/src/import-issue.shared.test.ts` parameterized over github/gitlab/gitea (`it(` count 19, copied/trimmed from `get-pull-request.shared.test.ts`, not imported from it).
- `@kaola/server` (#12 Issue import): `POST /api/v1/tasks/import` in `registerTasks` (same module as create — no new plugin, no webhook routes). Pre-publish draft: does **not** insert a `tasks` row, does **not** call `validateToken` (发布即校验 stays on existing `POST /api/v1/tasks`). Session cookie; gate `active`+`full` else `403` `{ error: 'forbidden' }`; unauthenticated uses existing `sendUnauthorized`. Body snake_case `{ issue_url, repo: { forge, base_url, full_name? }, credential }` with request `credential` `{ profile_id }` XOR `{ token }`. Generic parse → `400` `{ error: 'invalid_body' }` (no `message`). `repo.base_url` not http(s)+host → `400` `{ error: 'invalid_body', message: '仓库地址不是合法的 http 或 https 地址。' }`. Parses `issue_url` via `parseIssueUrl` **before** decrypt; unparseable → `400` `{ error: 'invalid_body', message: '无法解析 Issue 地址。' }` (zero fetch, no `token 揭示`). Optional `repo.full_name` must equal parsed `full_name` else `400` `{ error: 'invalid_body', message: 'Issue 地址与仓库不匹配。' }`. Profile bind is exact `===` on `forge` / `base_url` / **parsed** `full_name` (missing → `所选凭证档案不存在。`; mismatch → `所选凭证档案与仓库不匹配。`). Inline path does not encrypt (nothing is persisted). Vault miss on profile decrypt → `500` `{ error: 'vault_unconfigured' }`. Then `createForgeAdapter(repo.forge, { baseUrl: repo.base_url }).importIssue({ token }, issue_url)`. Forge 404/410 → `404` `{ error: 'issue_not_found', message: '无法读取该 Issue。' }`; forge 401 → `422` `{ error: 'token_check_failed', missing: ['读'], message: 'token 无效或无权读取该 Issue。' }`; other non-OK or network throw → `502` `{ error: 'forge_unreachable', message: '无法连接 forge 导入 Issue。' }`. Success **200** `{ title, description_md, source: { type: 'imported', issue_url }, repo: { forge, base_url, full_name } }` — not a Task Brief, not `201`, never a token / `token_encrypted` / `inline_token_encrypted` / `access_token`. Profile path writes `events.type` `token 揭示` after decrypt (including failures): `details` `{ profile_id, forge, base_url, full_name, outcome }` with `outcome` `ok` | `issue_not_found` | `token_check_failed` | `forge_unreachable` (`profile_id` integer PK; no `agent_key_id` / token / ciphertext). Inline path writes none. `insertTokenRevealEvent` outcome union gained `issue_not_found` (create path unchanged). New test file `apps/server/src/import.test.ts` (`test(` count 24). Forge token reveal channels remain REST claim `201` top-level `token` and MCP `claim_task` success `token`.
- `@kaola/web` (#12 Issue import): when 来源 is `imported`, button `data-testid="task-import"` label `导入` and `data-testid="task-import-source-label"` text exactly `导入内容`. Click → `POST /api/v1/tasks/import` with `Accept: application/json`, `credentials: 'include'`, snake_case `{ issue_url, repo: { forge, base_url }, credential }` from the current form (no `full_name` on the import request). 200 fills title / description / `task-repo` and keeps source imported. Failure shows server `message` or `导入失败（${status}）`; existing `发布失败（${status}）` copy is untouched. Board detail: `data-testid="board-detail-import-label"` text exactly `导入内容` iff `selectedTask.source.type === 'imported'`; existing `board-detail-issue-url` remains the source **link**. Description stays text interpolation (`{{ selectedTask.description_md }}`, no `v-html`). User still clicks 发布 → existing `POST /api/v1/tasks` + `validateToken`. Added 6 `it(` in `App.form.test.ts` and 1 `it(` in `App.board.test.ts` (existing cases untouched). Root `pnpm test` now includes `packages/forge-adapters/src/import-issue.shared.test.ts` (after `get-pull-request.shared.test.ts`) and `apps/server/src/import.test.ts` (after that). Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-12`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test`: ℹ tests 396  ℹ suites 79  ℹ pass 396  ℹ fail 0; vitest: Test Files 2 passed; Tests 51 passed (51); web vite v7.3.6, 2565 modules, `dist/assets/index-MeXOcpNK.js` 1,465.50 kB │ gzip: 406.21 kB (chunk-size warning only).
- `@kaola/forge-adapters` (#11 PR polling): `PrStatus` is no longer `unknown` — `{ state: 'open' | 'merged' | 'closed' }`. `getPullRequest(cred, prUrl)` is implemented (was `notImplemented`) on all three adapters. Parses owner/repo/number (GitHub `.../pull/{n}`, Gitea `.../pulls/{n}`) or namespace/iid (GitLab `.../-/merge_requests/{iid}`) out of the pasted PR/MR web URL; GitHub trailing `.diff`/`.patch` and a trailing slash on any kind are stripped first. Builds the REST endpoint: GitHub always `https://api.github.com/repos/{owner}/{repo}/pulls/{number}` (ignores `baseUrl`, same as `validateToken`); GitLab `{baseUrl}/api/v4/projects/{encodeURIComponent(namespace)}/merge_requests/{iid}`; Gitea `{baseUrl}/api/v1/repos/{owner}/{repo}/pulls/{number}` — GitLab/Gitea use the adapter's constructor `baseUrl`, never the prUrl's own host. Auth headers reuse `validateToken`'s per-kind scheme. State derivation: GitLab `state === 'merged'` → `merged`, `state === 'closed'` → `closed`, else (`opened`/`locked`) → `open`; GitHub/Gitea `merged === true` → `merged`, else `state === 'closed'` → `closed`, else `open`. Non-OK HTTP response or an unparseable prUrl rejects (unparseable rejects without calling `fetch`). New shared spec `packages/forge-adapters/src/get-pull-request.shared.test.ts` parameterized over github/gitlab/gitea (`it(` count 13, copied/trimmed from `validate-token.shared.test.ts`, not imported from it).
- `@kaola/server` (#11 PR polling): new `apps/server/src/poller.ts` exports `pollPendingReviews(db: AppDb): Promise<void>` — the only driver of `待验收` → `已完成`/`已退回`. Selects `tasks` where `status === '待验收'`; for each, reads the latest `submissions` row by `id` for that task (`orderBy(desc(submissions.id)).limit(1)`), decrypts the task's credential (profile or inline, same branch as `claimTask`) and calls the forge adapter's `getPullRequest({ token }, submission.prUrl)`. `state: 'open'` leaves the task untouched. `state: 'merged'` → `transitionTaskStatus('待验收', '已完成')`, `submissions.pr_state` set to `'merged'`. `state: 'closed'` → transition to `已退回`, `pr_state` set to `'closed'`. Both writes plus one `insertAuditEvent` run inside one `db.transaction`; event `type` `状态迁移`, `actorUserId: null` (system-driven, mirrors lease-expiry events), `details` `{ task_id, from: '待验收', to, pr_url }` (no `summary` key, unlike MCP `submit_pr`'s own `状态迁移` event). Never throws: a missing/undecryptable credential, an unreachable forge, a non-OK response, or a DB fault on one task's write phase is caught and only skips that task's row — the rest of the pending set is still polled, and `pollPendingReviews` itself always resolves (never rejects), which is required because it can be driven by `setInterval`. Tasks in any other status (`待认领`/`进行中`/`已完成`/`已取消`/already-`已退回`) are never selected and never fetched as PRs. The pre-existing poster `PATCH /api/v1/tasks/:publicId` `已退回` → `待认领` edge is unchanged and still works after a poller-driven `已退回`; prior `状态迁移` events and the `submissions` row survive that reopen unmodified. `vault.ts`'s `insertAuditEvent` first parameter is now typed as a structural `{ insert: AppDb['insert'] }` (not the full `AppDb`) so it also accepts a `db.transaction` handle. `buildApp({ pollIntervalMs? })`: new optional option, registered in its own child plugin (mirrors `mcp.ts`'s bearer context) so its `onClose` hook (which `clearInterval`s) runs before the root db-close hook regardless of registration order. Omitted or `<= 0` → no `setInterval` call at all. A positive integer → exactly one `setInterval(fn, pollIntervalMs)`, with an in-flight guard so an overrunning poll pass is never re-entered by the next tick; `app.close()` clears the timer. Production `index.ts` computes `pollIntervalMs` from `process.env.POLL_INTERVAL_MS` (unset or `''` → `60000`; otherwise `Number.parseInt(value, 10)`) and passes it into `buildApp`. Still no REST `submit_pr` and no webhook route — `pollPendingReviews` (on-demand in tests, or on the `pollIntervalMs` timer) is the only thing that ever moves a task out of `待验收`. New test file `apps/server/src/poller.test.ts` (`test(` count 9): drives a task to `待验收` through the real HTTP + MCP surface (create → Bearer claim → MCP `submit_pr`), then calls `pollPendingReviews(db)` against a second `createDb` connection to the same sqlite file — never against a stubbed `pollPendingReviews` or `buildApp`. Root `pnpm test` now includes `packages/forge-adapters/src/get-pull-request.shared.test.ts` (after `validate-token.shared.test.ts`) and `apps/server/src/poller.test.ts` (after `mcp.test.ts`). Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-11`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test`: ℹ tests 333  ℹ suites 70  ℹ pass 333  ℹ fail 0; vitest: Test Files 2 passed; Tests 44 passed (44); web vite v7.3.6, 2565 modules, `dist/assets/index-DtOsy8G8.js` 1,464.30 kB │ gzip: 406.06 kB (chunk-size warning only, unchanged from #10 since `@kaola/web` was not touched).
- `@kaola/server` (#10 MCP): `registerMcp(app, db)` in `buildApp` after `registerClaim` (before hosting). Dependencies `@modelcontextprotocol/sdk` `1.30.0` (exact) and `zod` `^4.4.3`. Encapsulated child uses `addAgentBearerHook` (same hook as whoami/claim; not `@fastify/bearer-auth`). Bearer `POST /api/mcp` Streamable HTTP (`StreamableHTTPServerTransport`, `enableJsonResponse: true`, stateful `mcp-session-id`); tests initialize with `protocolVersion` `2025-11-25`. `GET`/`DELETE /api/mcp` → `405` JSON-RPC `{ jsonrpc: '2.0', error: { code: -32000, message: 'Method not allowed.' }, id: null }`. Unauthenticated / wrong / non-Bearer / session-cookie-only → HTTP `401` `{ error: 'unauthorized' }` + `WWW-Authenticate: Bearer` before JSON-RPC. `McpServer` `{ name: 'kaola-tasks', version: '0.0.0' }`. Six `registerTool` names: `list_tasks`, `get_task_brief`, `claim_task`, `report_progress`, `release_task`, `submit_pr`. `claim_task` description includes `CLONE_TOKEN_USAGE` (`token 请通过环境变量或 git -c http.extraHeader 按次传递，不要写入 remote URL（会落盘到 .git/config）。`). Tools wrap extracted `claimTask` / `reportProgress` / `releaseTask` / `submitPr` from `claim.ts` (REST routes unchanged: claim/progress/release only; no REST `POST …/submit_pr`). `list_tasks` `{ tasks }` (optional `status` exact, `tags` membership of one tag, `forge` exact `repo.forge`); `get_task_brief` top-level brief; `claim_task` envelope keys `clone`/`lease`/`task`/`token`; `report_progress` `{ task, lease }`; `release_task` `{ task }`; `submit_pr` `{ task, pr_url, summary }` with `task.status` `待验收`. Business errors: JSON-RPC result `isError` + REST `{ error, message? }` in `structuredContent` (HTTP 200). Table `submissions` via `SUBMISSIONS_DDL` in `createDb`: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `task_id INTEGER NOT NULL` (integer `tasks.id`, not `public_id`), `lease_id INTEGER NOT NULL`, `pr_url TEXT NOT NULL`, `summary TEXT NOT NULL`, `pr_state TEXT NOT NULL`. Success `submit_pr` inserts `pr_state` `'open'`, marks the live lease `'released'`, writes `状态迁移` `{ task_id, from, to, pr_url, summary }`. `sweepExpiredLeases` on MCP list/get and mutating tools. Forge token reveal channels: REST claim `201` top-level `token` **and** MCP `claim_task` success `token`. Session GET list/get still never contain it. No PR polling. No web claim UI. Root `pnpm test` includes `apps/server/src/mcp.test.ts` after `claim.test.ts` (`test(` count 18 in that file). Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-10`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test`: ℹ tests 297  ℹ suites 60  ℹ pass 297  ℹ fail 0; vitest: Test Files 2 passed; Tests 44 passed (44); web vite v7.3.6, 2565 modules, `dist/assets/index-DtOsy8G8.js` 1,464.30 kB │ gzip: 406.06 kB (chunk-size warning only).
- `@kaola/server` (#9 REST claim): `registerClaim(app, db)` in `buildApp` after `registerTasks`. Table `leases` via `LEASES_DDL` in `createDb`: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `task_id INTEGER NOT NULL` (integer `tasks.id`, not `public_id`), `claimer_user_id INTEGER NOT NULL`, `agent_key_id INTEGER NOT NULL`, `claimed_at INTEGER NOT NULL`, `expires_at INTEGER NOT NULL`, `last_heartbeat INTEGER NOT NULL`, `state TEXT NOT NULL`. Unique index `leases_one_active_per_task` on `leases(task_id) WHERE state = 'active'`. Drizzle `state` `'active' | 'released' | 'expired'`. Default TTL `LEASE_TTL_SECONDS` `86400` (no per-task TTL column). Bearer hook `addAgentBearerHook` in `agent-bearer.ts` (encapsulated `onRequest`; not `@fastify/bearer-auth`); reused by whoami and claim plugins. Unauthenticated claim/progress/release → `401` `{ error: 'unauthorized' }` + `WWW-Authenticate: Bearer`. Routes (`Authorization: Bearer ktk_…`; session cookie does not authorize): `POST /api/v1/tasks/:publicId/claim` → `201` keys `clone`, `lease`, `task`, `token` (`task` = 15-key brief, `status` `进行中`; `token` = forge plaintext; `lease` `{ expires_at, ttl_seconds }` with `ttl_seconds` number `86400`; `clone` `{ suggested_dir, token_usage }` with `token_usage` exactly `token 请通过环境变量或 git -c http.extraHeader 按次传递，不要写入 remote URL（会落盘到 .git/config）。`); `POST …/progress` body `{ note?: string }` (omit body OK) → `200` `{ task, lease }` no token; `POST …/release` body `{ reason?: string }` (omit body OK) → `200` `{ task }` `status` `待认领` no token. Pending `待批准` claim → `403` `{ error: 'forbidden', message: '你的账号待正式成员批准后方可认领任务。' }`. Unknown `publicId` / numeric PK → `404` `{ error: 'not_found' }`. Second claim while `进行中` → `409` `{ error: 'conflict', message: '任务已被认领。' }`. Claim when status is not `待认领` → `409` `{ error: 'illegal_transition', message: '任务状态不允许从「${from}」变更为「进行中」。' }`. Progress/release by non-holder (`leases.claimer_user_id` !== Agent user id) → `403` `{ error: 'forbidden' }`. Progress/release with no live lease → `409` `{ error: 'conflict', message: '任务未被认领。' }`. Missing/invalid vault on claim decrypt → `500` `{ error: 'vault_unconfigured' }`. Events: claim `token 揭示` details `{ task_id, agent_key_id, credential: 'inline'|'profile', profile_id? }` then `状态迁移` `{ task_id, from, to }`; progress `心跳` `{ task_id, note }`; release `状态迁移` `{ task_id, from, to, reason? }`; expiry `状态迁移` `{ task_id, from: '进行中', to: '待认领' }` `actor_user_id` null. `insertAuditEvent` accepts `actorUserId: number | null`. Expiry via `sweepExpiredLeases` check-on-read (session GET list/one) and check-on-write (claim/progress/release). No cron. Session GET list/get still never contain forge token. As of #9 the HTTP that returns a forge token is successful claim `201` top-level `token` (#10 also reveals via MCP `claim_task`). MCP, `submit_pr`, and web claim UI not implemented in #9. Root `pnpm test` includes `apps/server/src/claim.test.ts` after `hosting.test.ts`. Measured 2026-08-21 (`CI=true`) in worktree `.kw/worktrees/issue-9`: `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; node `--test`: ℹ tests 279  ℹ suites 54  ℹ pass 279  ℹ fail 0; vitest: Test Files 2 passed; Tests 44 passed (44); web vite v7.3.6, 2565 modules, `dist/assets/index-DtOsy8G8.js` 1,464.30 kB │ gzip: 406.06 kB (chunk-size warning only).
- `@kaola/web` (#8 board UI): member workbench (`view === 'member'`) Chinese 任务看板 with 列表/看板, client-side filters (状态 / 标签 / Forge), detail + one synthetic 发布 timeline from `created_at`+`poster`. Loads `GET /api/v1/tasks` exactly (no query). No events HTTP. No vue-router. Pending users see 账号待批准 (no board). `claim_only` sees the board, not the 发布任务 form (`canApprove` = `active`+`full`). Tests: `apps/web/src/App.board.test.ts` plus existing `App.form.test.ts` (vitest `include` `src/**/*.test.ts`).
- `@kaola/server` (#17 single-port 31415): `index.ts` `PORT` default `'31415'`, `HOST` `'0.0.0.0'`, `SQLITE_PATH` `':memory:'`; passes `WEB_DIST` and `VITE_DEV_TARGET` into `buildApp({ sqlitePath?, webDist?, viteDevTarget? })`. Omit/empty both hosting options → `GET /` `text/plain; charset=utf-8` body `考拉任务服务占位` (`getPlaceholderBody()`). Non-empty `webDist` → `@fastify/static` `^10.1.3` + exact `GET /` `sendFile` `index.html` + SPA fallback for other GET except `/api` and `/login*`. Both set → `webDist` wins. Only `viteDevTarget` → `@fastify/http-proxy` `^11.6.0`. `auth.ts` `PUBLIC_URL` default `'http://localhost:31415'` (trailing slash stripped). Post-login still `reply.redirect('/')`. Callbacks still `${publicUrl}/login/{github|gitlab|gitea}/callback`. `registerAuth` required env unchanged (nine vars). Tests: `apps/server/src/hosting.test.ts`.
- Root (#17): `"dev": "node scripts/dev.mjs"` — Fastify `:31415` + Vite `127.0.0.1:5173 --strictPort` with `VITE_DEV_TARGET` (default `http://127.0.0.1:5173`). Advertised origin is 31415; Vite 5173 is loopback-only, not the advertised origin. `apps/web/vite.config.ts` proxy `/api` and `/login` → `http://127.0.0.1:31415`. `docker-compose.yml` `"31415:31415"`, `PORT: "31415"`, `HOST: 0.0.0.0`. `apps/server/Dockerfile` `RUN pnpm --filter @kaola/web build`, `ENV PORT=31415`, `ENV HOST=0.0.0.0`, `ENV WEB_DIST=/app/apps/web/dist`, `EXPOSE 31415`. Root `pnpm test` includes `apps/server/src/hosting.test.ts` then `&& pnpm --filter @kaola/web test`.
- `@kaola/shared` (#7): `taskBriefSchema.credential` is `z.union` of two `z.strictObject` members `{ profile_id: z.string() }` and `{ inline: z.literal(true) }`. Token keys / extra keys / `{ inline: false }` / both forms at once are rejected.
- `@kaola/server` (#7 tasks): first workspace deps `"@kaola/shared": "workspace:*"`, `"@kaola/forge-adapters": "workspace:*"`. Imports package export `createForgeAdapter`; `validateToken` is `adapter.validateToken`, not a package-level export. Table `tasks` via `TASKS_DDL` in `createDb`: INTEGER PK `id`, `public_id` TEXT NOT NULL UNIQUE (`kt-YYYY-NNNN`), `title`, `description_md` DEFAULT `''`, `source_type`, `source_issue_url`, `repo_forge`, `repo_base_url`, `repo_full_name`, `repo_base_branch`, `repo_suggested_dir`, `acceptance_criteria` DEFAULT `'[]'`, `test_command` DEFAULT `''`, `allowed_paths` DEFAULT `'[]'`, `forbidden_paths` DEFAULT `'[]'`, `priority`, `tags` DEFAULT `'[]'`, `credential_profile_id`, `inline_token_encrypted`, `poster_user_id`, `status`, `created_at`; CONSTRAINT `tasks_credential_xor` CHECK `((credential_profile_id IS NULL) != (inline_token_encrypted IS NULL))`. `registerTasks(app, db)` in `buildApp`. Session HTTP: `GET /api/v1/tasks` → `200` `{ tasks: [<brief>] }` (any logged-in user including `待批准`); `GET /api/v1/tasks/:publicId` → `200` brief or `404` `{ error: 'not_found' }` (addressed by `public_id` string, not integer PK); `POST /api/v1/tasks` (gate `active`+`full`) snake_case body, request `credential` `{ profile_id }` XOR `{ token }` → `201` brief (`status` `待认领`; brief `credential` `{ profile_id: "<id>" }` or `{ inline: true }`; `pr_convention.branch_prefix` `kaola/${id}-`, `title_prefix` `[${id}] `). Profile path binds `repo.forge`/`base_url`/`full_name` to the profile row (exact `===`) before decrypt; mismatch → `400` `{ error: 'invalid_body', message: '所选凭证档案与仓库不匹配。' }`; missing profile → `400` `{ error: 'invalid_body', message: '所选凭证档案不存在。' }`. `repo.base_url` must be http or https with a host; else `400` `{ error: 'invalid_body', message: '仓库地址不是合法的 http 或 https 地址。' }`. 发布即校验 via `adapter.validateToken`: `422` `{ error: 'token_check_failed', missing, message }` (`missing` includes `读` → `token 无效或无权访问该仓库，任务未发布。`; else `token 权限不足：缺少 … 权限，任务未发布。`); `502` `{ error: 'forge_unreachable', message: '无法连接 forge 校验 token，任务未发布。' }`; missing/invalid vault → `500` `{ error: 'vault_unconfigured' }`. `PATCH /api/v1/tasks/:publicId` poster only (`403` otherwise): `待认领`→`已取消`; `已退回`→`已取消`|`待认领`; illegal → `409` `{ error: 'illegal_transition', message: '任务状态不允许从「…」变更为「…」。' }`. Profile decrypt writes `events.type` `token 揭示` details `{ profile_id, forge, base_url, full_name, outcome }` (`ok` | `token_check_failed` | `forge_unreachable`); no token in details; inline path does not write this event. PATCH writes `状态迁移` details `{ task_id, from, to }`. Task CRUD responses contain no token. `GET /` still `考拉任务服务占位`. MCP still unimplemented (REST claim is #9).
- `@kaola/web` (#7): Chinese 发布任务 form gated by `canApprove` (`active`+`full`); profile dropdown reuses loaded `profiles`; two credential paths (`{ profile_id }` / `{ token }`); create only — no board/kanban/edit. `package.json` `"test": "vitest run"`; devDeps `@vue/test-utils` `^2.4.11`, `happy-dom` `^20.11.6`, `vitest` `^4.1.11`. `vite.config.ts` `test.environment` `happy-dom`, `include` `src/**/*.test.ts`.
- Root `pnpm test`: `node --experimental-strip-types --test packages/shared/src/index.test.ts packages/forge-adapters/src/index.test.ts packages/forge-adapters/src/validate-token.shared.test.ts apps/server/src/placeholder.test.ts apps/server/src/auth.test.ts apps/server/src/agent-keys.test.ts apps/server/src/vault.test.ts apps/server/src/tasks.test.ts && pnpm --filter @kaola/web test`. Measured 2026-08-21 (`CI=true`): `pnpm lint` exit 0 (`eslint .`); `pnpm typecheck` exit 0 (4 of 5 workspace projects); `pnpm test` exit 0 (node `--test`: ℹ tests 243  ℹ suites 43  ℹ pass 243  ℹ fail 0; vitest: Test Files 1 passed; Tests 27 passed (27)); `pnpm build` exit 0 (web vite v7.3.6, 2565 modules, `dist/assets/index-I8OsCCpl.js` 1,459.50 kB │ gzip: 404.84 kB; chunk-size warning only).
- `@kaola/server` (#4 Agent API keys): tables `agent_keys` (`id`, `user_id`, `key_hash` NOT NULL UNIQUE, `label` NOT NULL DEFAULT '', `last_used_at` INTEGER NULL) via `CREATE TABLE IF NOT EXISTS` in `createDb`. Session HTTP: `POST /api/v1/agent-keys` body `{ "label"?: string }` → `201` `{ id, label, token, last_used_at: null }` (`token` only here; plaintext `ktk_` + 64 hex); `GET /api/v1/agent-keys` → `200` `{ keys: [{ id, label, last_used_at }] }` (no `token`, no `key_hash`); `DELETE /api/v1/agent-keys/:id` → `200` `{ ok: true }`; missing/not-owned → `404` `{ error: 'not_found' }`. Generate/list/revoke require `status === 'active'` (approved GitHub `claim_only` may manage own keys). Pending `POST` → `403` `{ error: 'forbidden', message: '你的账号待正式成员批准后方可生成 Agent Key。' }`; pending `GET`/`DELETE` → `403` `{ error: 'forbidden' }`. Unauthenticated session routes use the same oracle as `GET /api/v1/me` (`401` `{ error: 'unauthorized' }` or `302` `/login`). Bearer `GET /api/v1/agent/whoami` (child Fastify `onRequest`; `Authorization` scheme case-insensitive `Bearer`) → `200` `{ id, key_id, label, status, permission_level }` (`id` = user id); `401` `{ error: 'unauthorized' }` with `WWW-Authenticate: Bearer`. `key_hash` is sha256 hex of plaintext (`utf8`). `last_used_at` is unix seconds and ticks only on successful Bearer auth. No `@fastify/bearer-auth`; `node:crypto` only (no new npm dependency). Does not accept Bearer on `GET /api/v1/me`.
- `@kaola/server` (#5 token vault / credential profiles): tables `credential_profiles` (`id`, `forge`, `base_url`, `repo_full_name`, `token_encrypted`, `scopes_checked` TEXT NOT NULL DEFAULT `'[]'`, `created_by`; UNIQUE `(forge, base_url, repo_full_name)`) and `events` (`id`, `type`, `actor_user_id`, `created_at`, `details`) via `CREATE TABLE IF NOT EXISTS`. AES-256-GCM (`aes-256-gcm`, `{ authTagLength: 16 }`, 12-byte IV) layout `iv||ciphertext||tag` stored as base64 TEXT. Env `VAULT_MASTER_KEY` 64 hex chars required at `encryptToken`/`decryptToken`, not at `buildApp()` boot. Session HTTP (gate `status === 'active'` AND `permission_level === 'full'`): `GET /api/v1/credential-profiles` → `200` `{ profiles: [{ id, forge, base_url, repo_full_name, scopes_checked, created_by }] }` (never token / `token_encrypted`); `POST /api/v1/credential-profiles` body `{ forge, base_url, repo_full_name, token }` → `201` same shape as a list item (`scopes_checked` starts `[]`; does not call `validateToken`); duplicate unique triple → `409` `{ error: 'conflict' }`; invalid body → `400` `{ error: 'invalid_body' }`; missing/invalid vault key → `500` `{ error: 'vault_unconfigured' }`; `DELETE /api/v1/credential-profiles/:id` → `200` `{ ok: true, message: '请同时到 forge 侧撤销该 token。' }`; missing → `404` `{ error: 'not_found' }`; `claim_only` or pending → `403` `{ error: 'forbidden' }`. `forge` enum `github` | `gitlab` | `gitea`. Audit: create/delete write `events.type` `变更` with `details` `{"action":"create"|"delete","profile_id":<n>}`; module export `revealCredentialProfile(db, { profileId, actorUserId, agentKeyId })` returns plaintext and writes `type` `token 揭示` with `details` `{"agent_key_id":<n>,"profile_id":<n>}`. `revealCredentialProfile` is not HTTP; the HTTP that returns a forge token is Bearer `POST /api/v1/tasks/:publicId/claim` `201` top-level `token` (#9). After delete, reveal throws. `GET /` still `考拉任务服务占位`. At #5, MCP, task CRUD, claim, and `tasks` table were still unimplemented. Server still does not import `@kaola/forge-adapters`.
- `@kaola/web` (#4/#5): Agent Key widget when `status === 'active'`; credential-profile widget when `active` && `full`. Chinese copy; create key shows plaintext once; delete profile shows `请同时到 forge 侧撤销该 token。`. No vue-router.
- Root `pnpm test` file list: `packages/shared/src/index.test.ts`, `packages/forge-adapters/src/index.test.ts`, `packages/forge-adapters/src/validate-token.shared.test.ts`, `apps/server/src/placeholder.test.ts`, `apps/server/src/auth.test.ts`, `apps/server/src/agent-keys.test.ts`, `apps/server/src/vault.test.ts`. Measured 2026-08-21 (`CI=true`): `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; tests 149 pass / 0 fail, 26 suites; web vite `2565` modules, `dist/assets/index-BCXNNTa7.js` 1,452.16 kB gzip 402.89 kB (chunk-size warning only).
- `@kaola/server`: multi-source OAuth (GitHub / GitLab / Gitea) with `@fastify/oauth2@^8.3.0`, `@fastify/cookie@^11.1.2`, `@fastify/session@^11.1.2`. `users` table columns `id`, `provider`, `remote_id`, `username`, `display_name`, `status`, `permission_level`; UNIQUE `(provider, remote_id)`. `status` `active` | `待批准`; `permission_level` `full` | `claim_only`; `provider` `github` | `gitlab` | `gitea`. HTTP: `GET /login`; OAuth start `GET /login/github`, `/login/gitlab`, `/login/gitea`; callbacks `GET /login/github/callback`, `/login/gitlab/callback`, `/login/gitea/callback`; `GET /api/v1/me`; `POST /api/v1/users/:id/approve` (actor must be `active` + `full`; sets target `status` to `active`; GitHub stays `claim_only`). Pending `GET /api/v1/me` includes `message` `你的账号待正式成员批准后方可认领任务。`. `GET /` still `text/plain; charset=utf-8` body `考拉任务服务占位`. `registerAuth` requires non-empty `SESSION_SECRET`, `OAUTH_GITHUB_CLIENT_ID`, `OAUTH_GITHUB_CLIENT_SECRET`, `OAUTH_GITLAB_CLIENT_ID`, `OAUTH_GITLAB_CLIENT_SECRET`, `OAUTH_GITLAB_BASE_URL`, `OAUTH_GITEA_CLIENT_ID`, `OAUTH_GITEA_CLIENT_SECRET`, `OAUTH_GITEA_BASE_URL`. Optional `PUBLIC_URL` default `http://localhost:3000`. `SQLITE_PATH` default `:memory:`. MCP, task CRUD, vault, and claim still unimplemented.
- `@kaola/web`: Chinese Naive UI (`zhCN`) login buttons to `/login/github|gitlab|gitea`; pending card; 正式成员 approve by user id. Vite proxy `/api` and `/login` → `http://127.0.0.1:3000`. No vue-router.
- `@kaola/forge-adapters`: `createForgeAdapter(kind, options?: { baseUrl?: string })` implements `kind` + `validateToken` (global `fetch`, GET-only). Types: `ForgeKind`, `Credential` `{ token: string }`, `RepoRef` `{ full_name: string; base_url: string }`, `TokenCapability` `'读'|'推'|'PR'`, `TokenCheck` `{ missing: TokenCapability[] }`, `CreateForgeAdapterOptions`, `ForgeAdapter`. Placeholders `ImportedIssue`, `PrStatus`, `ForgeEvent`, `IssueRef` are `unknown`. Other interface methods throw `Error('not implemented')`. GitHub API host always `https://api.github.com` (ignores `baseUrl`); GitLab join strips trailing slashes then `/api/v4`; Gitea `/api/v1`. No runtime HTTP dependency. Push/PR checks are REST permission proxies, not mutating git push / POST PR. `getForgeAdaptersHealth()` still returns `kaola-forge-adapters-ready`.
- Root `pnpm test` file list: `packages/shared/src/index.test.ts`, `packages/forge-adapters/src/index.test.ts`, `packages/forge-adapters/src/validate-token.shared.test.ts`, `apps/server/src/placeholder.test.ts`, `apps/server/src/auth.test.ts`. Measured 2026-08-21 (`CI=true`): `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` exit 0; tests 124 pass / 0 fail, 10 suites; web vite `2565` modules, `dist/assets/index-CfV3Ee1H.js` 1,445.96 kB gzip 401.17 kB (chunk-size warning only).
- `@kaola/shared`: Task Brief zod schema (`taskStatusSchema`, `type TaskStatus`, `taskBriefSchema`, `type TaskBrief`, `parseTaskBrief`) and `transitionTaskStatus` matching [DESIGN.md](docs/DESIGN.md) §5–§6. `getSharedHealth()` still returns `kaola-shared-ready`. Dependency `zod` `^4.4.3` (lockfile `zod@4.4.3`). DESIGN.md §6 example still parses (tests pin it). HTTP `GET /`, MCP, and `@kaola/forge-adapters` unchanged.
- M0 scaffold: pnpm workspaces (`apps/*` + `packages/*`) with `@kaola/web` (Vue 3 + Vite + Naive UI placeholder), `@kaola/server` (Fastify + drizzle-orm + better-sqlite3), `@kaola/shared` (`getSharedHealth()` → `kaola-shared-ready`), `@kaola/forge-adapters` (`getForgeAdaptersHealth()` → `kaola-forge-adapters-ready`). Root scripts: `pnpm install`, `pnpm lint` (`eslint .`), `pnpm typecheck` (`pnpm -r --if-present typecheck`), `pnpm test` (`node --experimental-strip-types --test` on `packages/shared/src/index.test.ts`, `packages/forge-adapters/src/index.test.ts`, `apps/server/src/placeholder.test.ts`), `pnpm build` (`pnpm -r --if-present build`). Server: `pnpm --filter @kaola/server start` listens `HOST` default `0.0.0.0`, `PORT` default `3000`; `GET /` body `考拉任务服务占位`. `docker-compose.yml` skeleton (service `server`, port 3000). CI job `lint-test` on Node 22 (`pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm test`).
- Initialized Kaola-Workflow documentation structure.
- Added design document v0.1 (`docs/DESIGN.md`).
- Design v0.2: multi-provider login with tiered permissions (GitHub = claim-only + first-login approval), claim-as-authorization, agent-side token hygiene, `suggested_dir` in task brief.
