services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "31415:31415"
    environment:
      PORT: "31415"
      HOST: "0.0.0.0"
    volumes:
      - kaola-data:/data

volumes:
  kaola-data:
