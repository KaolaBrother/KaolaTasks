# Goal

Deliver the two-mode Kaola MCP installation and certificate-trust onboarding contract, implementation, README guidance, and honest cross-platform evidence for Issue #48 without mixing the co-active Issue #46 branch or committing real deployment identifiers.

- item: Reconcile the merged Issue #46 TLS contract into this branch, then freeze the public-CA and private-CA installation, trust-bootstrap, privilege, removal, and rotation contract in DESIGN and README.
  status: done
  dispatched: Grok CLI via exact Runner session `grok-kaola-issue-48` → `/Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-48`; wait for Issue #46 dependency truth, then edit only the Issue #48 worktree and leave reviewable Git and validation evidence there.
  result: docs/DESIGN.md §16 and README.md「安装与证书信任」 freeze the two operator schemes only. Unimplemented CLI/state/storage contracts removed (no `kaola-mcp trust …`, no `KAOLA_HOME/trust/`, no state file). #46 still open; no copy of its worktree or server issuance. Public-CA = strict default trust, no extra CA, no `NODE_EXTRA_CA_CERTS`. Private-CA = every enrolled computer trusts the public root PEM; MCP-only `NODE_EXTRA_CA_CERTS` after `openssl x509 -fingerprint -sha256`; browser/system trust is a separate explicit elevation. First connection must not TOFU a server-returned CA. Restart, uninstall, rotation, leave-team, and migrate-to-public-CA are documented. Root private key is never distributed. Placeholders only (`<kaola-origin>`, `<dev-root-ca.pem>`, `<sha256-fingerprint>`). Installer/code/tests remain missions 2–3. Working-tree `git diff --check` exit 0; `pnpm lint` exit 0. Added-line scan of product docs `README.md` + `docs/DESIGN.md` vs a9de0fb: no SHA-256 hex, no `/Users/`, no PEM blocks, no DNS vendors; only pre-existing `127.0.0.1:31415` and public GitHub issue links. Workflow metadata (`kaola-workflow/issue-48/workflow-state.md` and this file's `dispatched` locator) retains standard Kaola Workflow absolute local custody paths and is excluded from the deployment-identifier scan.

- item: Implement the bounded MCP installation and trust-onboarding surfaces with fail-closed fingerprint or signature verification, explicit system-trust elevation, uninstall behavior, and focused automated acceptance.
  status: done
  dispatched: code-reviewer (spawn_subagent, cwd Issue #48 worktree) → review exact candidate bytes of `apps/mcp/src/trust.ts`, `apps/mcp/src/main.ts` extra-CA HTTPS path, and `apps/mcp/src/trust.test.ts` oracle. Independent focused run already 17/17 pass at this freeze. Do not touch Issue #46. Do not weaken tests.
  result: Oracle `apps/mcp/src/trust.test.ts` (wired in root package.json) RED then green. Production `apps/mcp/src/trust.ts` (verify/install/status/uninstall/export/system-plan, never spawn) + `apps/mcp/src/main.ts` applies `env.NODE_EXTRA_CA_CERTS` on HTTPS by adding the PEM to default roots via `node:https.request` (no `rejectUnauthorized: false`; Node 22.0–22.14 safe `tls` namespace fallback). Focused `node --experimental-strip-types --test apps/mcp/src/trust.test.ts` 17/17 pass; `@kaola/mcp` typecheck 0; eslint on those files 0. code-reviewer pass after R1 import fix. Docs: DESIGN §16.6 + README「安装与证书信任」+ CHANGELOG Unreleased `#48`. No installer CLI. No Issue #46 worktree edits. No real deployment identifiers. Reviewable commit on `workflow/issue-48`.

- item: Validate the frozen candidate with repository gates, secret and environment-identifier scans, independent security review, and only the macOS, Windows, Linux, browser, and MCP checks actually executed.
  status: done
  dispatched: investigator → kaola-workflow/issue-48/validation-gates.md (pnpm lint/typecheck/test/build, secret+deployment-identifier scan of product files vs origin/main, honest executed vs not-executed macOS/Windows/Linux/browser/MCP checks); security-reviewer → kaola-workflow/issue-48/security-review.md (independent review of apps/mcp/src/trust.ts + apps/mcp/src/main.ts extra-CA HTTPS path at HEAD 5c08145). cwd `/Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-48`. Do not touch Issue #46 worktree. Do not mutate product files. Do not run elevation cert-install commands. Live public-CA / private-CA / browser OAuth UAT is 配合 and must be recorded as not executed unless actually run.
  result: BLOCKED. Codex review found an independent causal class that invalidates validating the previous freeze: (1) Issue #48 requires a user-callable installer that stores a fingerprint-or-signature-verified PEM under the user Kaola directory, but only internal `installRootCa()` exists and README still says the installer CLI is undelivered; (2) `runStdioBridge` `extraCaPemFromBridgeEnv()` is structural-only and honors caller `NODE_EXTRA_CA_CERTS`, so a replaced on-disk CA is still trusted and `STABLE_PUBLIC_CA` cannot refuse unexpected extra CA; focused tests only prove replacement fail-closed via `exportMcpTrustEnv(expectedFingerprint)`, not the real bin/launcher startup chain. Investigator measurements landed at `kaola-workflow/issue-48/validation-gates.md` against HEAD `5c08145` but are not a PASS of this mission. security-reviewer dispatch cancelled. Repair is a new mission. Do not finalize, merge, archive, or close Issue #48.

- item: Deliver a user-callable `kaola-mcp trust` installer and fail-closed launcher so fingerprint/signature verification, host-neutral state, replacement, public-mode extra-CA refusal, uninstall, rotation/restart, and real child-bridge injection hold on the package-bin startup path.
  status: done
  dispatched: tdd-guide → `apps/mcp/src/trust-cli.test.ts` RED 14 fail / 0 pass at baseline `5c08145` (wired in root package.json `test`; do not weaken). implementer → production so package bin `apps/mcp/bin/kaola-mcp.mjs` satisfies that oracle (`apps/mcp/src/trust.ts` state+manifest+permissions, `apps/mcp/src/main.ts` trust CLI dispatch + launcher fail-closed before HTTP). cwd `/Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-48`. Do not edit the oracle. Do not touch Issue #46. Do not commit.
  result: DESIGN §16.7 freezes `kaola-mcp trust install|status|uninstall|system-plan` plus launcher injection. Oracle `apps/mcp/src/trust-cli.test.ts` RED 14/14 on `5c08145`, then green 14/14. Production: `trust.ts` writes `$KAOLA_HOME/trust/root-ca.pem` + host-neutral `state.json` (0700/0600, fingerprint or Ed25519 manifest); `main.ts` direct-run dispatches CLI and fail-closed `resolveLauncherTrust` before HTTP; public mode refuses caller `NODE_EXTRA_CA_CERTS`. Independent re-run: trust-cli 14/14, trust.test 17/17. Gates after docs: `pnpm lint` 0 (after unused-helper mechanical drop in oracle), `pnpm typecheck` 0, `pnpm test` 874+131, `pnpm build` 0. README/api/smoke-test/CHANGELOG docked with placeholders only. No Issue #46 edits. No merge/archive/finalize/close. PR #50 to be updated by push.
