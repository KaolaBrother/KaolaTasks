# Goal

Deliver Issue #53 in full on branch `workflow/bundle-53` (worktree `/Volumes/WorkspaceA/ylminiserver/workspace/KaolaTasks/.kw/worktrees/bundle-53`): the in-Kaola review loop — Draft PR delivery, multi-round review discussion, the new `待修改` / `待合并` states, revision Claims via `submit_revision`, four new MCP tools, six review REST routes, `parent_task_id` dependent sub-tasks with automatic restack, ForgeAdapter `markPullRequestReady` / `commentOnPullRequest` / extended `getPullRequest`, a server SSE stream, and the Chinese Web board/review panel — with phases 0–7 acceptance items, all repo gates green, zero token leakage into any new surface, and honest 配合 marking for anything not actually executed.

- item: Phase 0 — freeze the design contract: DESIGN.md v0.6 header + D9–D17 in §2, the §5 state machine (new `待修改`/`待合并`, narrowed `已退回`), §6 `pr_convention.draft` / `parent_task_id` / derived `base_branch`, §8 adapter methods, §9 new tools and parameters, §10 new tables and event types; api.md routes and error codes for every new tool/route; architecture.md sync.
  status: done
  dispatched: self → edit `docs/DESIGN.md`, `docs/api.md`, `docs/architecture.md` in the bundle-53 worktree; commit on `workflow/bundle-53`.
  result: Committed on `workflow/bundle-53` (docs: freeze in-Kaola review loop contract). DESIGN.md header → v0.6 (2026-09-07) with a #53 summary; §2 rows D9–D17; §5 mermaid + eight canonical statuses + per-edge table, `已退回` narrowed, `待验收 → 已完成` removed, rule text now「验收判定在考拉，合并在 forge」; §6 `pr_convention.draft`, `parent_task_id`, `review_round`, derived `base_branch`; §7 in-use credential list includes 待修改/待合并; §8 interface + `PrStatus` extension + method behaviours; §9 tool table (six existing + four new) and Review Brief; §10 new tables/columns/event types; new §17 (分工、评审 REST、Agent 侧、子任务/restack、SSE、不变式). api.md: review REST section with error codes per route, SSE, MCP rows for the four tools, `report_progress` percent/phase, submissions/new tables, events, `getPullRequest` fields, `markPullRequestReady` / `commentOnPullRequest`, shared edges. architecture.md route map updated. Summary comment on approve is documented as opt-in via `KAOLA_REVIEW_SUMMARY_COMMENT=1` (open question 3 resolved as default-off).

- item: Phase 1 — shared package and storage: `taskStatusSchema` + `LEGAL_TRANSITIONS` per §5, `taskBriefSchema` `parent_task_id` / `pr_convention.draft`, `createDb` new tables (`submission_revisions`, `review_rounds`, `discussion_messages`) and additive columns (`tasks.parent_task_id`, `submissions.head_sha` / `is_draft` / `review_round`), with per-edge transition tests and an old-SQLite-file migration test that proves existing submissions keep their rows with `review_round` 0 / `is_draft` false / `head_sha` null; `pnpm typecheck` green.
  status: done
  dispatched: self → `packages/shared/src/index.ts` + `index.test.ts`, `apps/server/src/schema.ts`, `apps/server/src/db.ts`, `apps/server/src/db-migration.test.ts` in the bundle-53 worktree; commit on `workflow/bundle-53`.
  result: Commit a56398f. Shared: eight statuses, `LEGAL_TRANSITIONS` per §5 table (16 edges; `待验收 → 已完成` retired), brief `parent_task_id` / `review_round` / `pr_convention.draft`, exported `discussionMessageKindSchema` / `reviewRoundKindSchema` / `reviewVerdictSchema`; matrix test covers all 64 pairs (159/159 pass). Server: `tasks.parent_task_id`, `submissions.head_sha/head_branch/is_draft/review_round`, `submission_revisions` (lease_id unique), `review_rounds` ((task_id, round) unique), `discussion_messages` via `tryAddColumn` / `CREATE IF NOT EXISTS`; `taskBrief` emits the new keys and `selectTask(s)` derive `base_branch` from the parent's PR head branch (`deriveBaseBranch`); poster may cancel `待修改`. `db-migration.test.ts` gains two #53 cases (pre-#53 file keeps its submission/task rows with `review_round` 0, `is_draft` 0, `head_sha` null, `parent_task_id` null; idempotent reopen). `pnpm typecheck` 0, `pnpm lint` 0. Known red until Phase 2: poller / webhook / writeback suites still drive `待验收 → 已完成` (14 failures), which Phase 2's poller rewrite owns.

- item: Phase 2 — server review core: review_rounds / discussion_messages / submission_revisions read-write, the six review REST routes (`GET review`, `POST messages`, `POST rounds`, `POST approve`, `POST withdraw`, `POST terminate`) gated to `active` `admin`/`full`, `claim_task` accepting `待修改`, `release_task` and lease expiry returning to `待修改` when a submission exists, `submit_pr` first-only (`409 use_submit_revision`), `submitRevision` service (`422 pr_url_invalid`, `409 head_sha_unchanged`, idempotent repeat), poller and webhook driving `待合并` to `已完成`/`已退回` and `待修改` closed-PR to `已退回`; acceptance tests for the full multi-round loop and every error path; no token in any review response, `events.details`, or log.
  status: done
  dispatched: self → new `apps/server/src/review.ts` (+ `review.test.ts`), edits to `claim.ts`, `leases.ts`, `poller.ts`, `webhook.ts`, `app.ts`; legacy fixture premise update (poller / webhook / writeback / claim-fencing / mcp / workflow-default tests) → implementer subagent `fixtures-53` (opus), no commit; commit on `workflow/bundle-53` in the bundle-53 worktree.
  result: Commits 2497051 (production + `review.test.ts`) and the fixture commit (test: re-premise merge completion on 待合并 …). `review.ts` implements the six REST routes (reviewer = `canPublish`; 401/403/404/409/400 per api.md), Review Brief, `reviewerSubmitRound` (blocking → `待修改` + `changes_requested`, else round-only; `409 no_pending_messages`), approve (`待验收 → 待合并`, `评审通过`, async Draft→ready), withdraw, terminate (three states → `已退回`), `submitRevision` (`422 pr_url_invalid`, `409 head_sha_unchanged`, `409 use_submit_pr`, idempotent repeat, lease released, `submission_revisions` + `review_rounds.revised_at`, `评审交回`), `postDiscussionMessage` (active-Claim fencing → `stale_claim`, `resolves` must be a blocking message of the task), `getReviewFeedback`; `claim.ts` accepts `待修改` (CAS on `from`), `release_task` and `sweepExpiredLeases` return to `待修改` once a submission exists, `submit_pr` is first-only (`409 use_submit_revision`); poller/webhook cover 待验收/待修改/待合并 with `prTerminalTarget` (merged only from 待合并; closed from all three). `review.test.ts` 9/9: full loop with the seven-edge 状态迁移 chain asserted in order and a needle scan for the token / ciphertext keys over every response and `events`. Legacy suites re-premised by `fixtures-53` (diff reviewed: only `forceStatus(…, '待合并')` before merges, expected `from` → `待合并`, tool count 6 → 10, plus two new negative tests that a merge while `待验收` leaves the task and `pr_state` untouched — poller and webhook). Full server run 1016/1016.

- item: Phase 3 — MCP tools: register `get_review_feedback`, `post_discussion_message`, `submit_revision`, `open_review_round`; `report_progress` optional `percent` (0–100 int, `400` out of range) and `phase`; `list_tasks` returning `parent_task_id` / `review_round`; `KAOLA_MCP_INSTRUCTIONS` addendum (claim `待修改` → `get_review_feedback` first; Draft PR convention; finish with `submit_revision`); MCP-level tests walking the phase-2 loop Agent-side only through MCP, `blocking[].resolved` after `kind=resolution`, `409 stale_claim` without an active Claim, and a serialized-output token scan.
  status: done
  dispatched: self → `apps/server/src/mcp.ts` (+ coverage inside `review.test.ts` via the real Streamable HTTP MCP client); commit on `workflow/bundle-53`.
  result: Commit 2497051. Four tools registered after `submit_pr` (`submit_revision`, `get_review_feedback`, `post_discussion_message`, `open_review_round`) with zod input schemas mirroring api.md; `report_progress` gains `percent` / `phase` (invalid → `isError invalid_body`), `submit_pr` gains `head_sha`, `list_tasks` briefs carry `parent_task_id` / `review_round`; `KAOLA_MCP_INSTRUCTIONS` gains the 评审循环 paragraph (Draft PR, claim `待修改` → `get_review_feedback` first, same PR, `submit_revision`, percent/phase, sub-task base branch, `source_trust`). `review.test.ts` drives the Agent side only through MCP: revision claim → `get_review_feedback` shape pinned to the 11 Review Brief keys → `post_discussion_message` resolution flips `blocking[].resolved` → `submit_revision` → repeat idempotent → stale Claim `409 stale_claim`; `open_review_round` `403` when the Claim is not on a child; percent 500 → `invalid_body`; every tool body needle-scanned. `mcp.test.ts` / `workflow-default.test.ts` pin the ten-tool list and that no tool gains a carrier/runner field.

- item: Phase 4 — ForgeAdapter: `getPullRequest` returns `{ state, head_sha, draft, head_branch }`; new `markPullRequestReady` (GitHub GraphQL `markPullRequestReadyForReview`, GitLab/Gitea title prefix strip via PUT) and `commentOnPullRequest`, both under the #37 `timeoutMs` contract and the `${method}: ${kind} responded ${status}` throw idiom; shared specs (`get-pull-request` extended, new `mark-pull-request-ready.shared.test.ts` and `comment-on-pull-request.shared.test.ts`) run against all three stub backends; server side records `head_sha` / `is_draft` / `head_branch` on submit or first poll and, after 「通过」, flips Draft → ready asynchronously in the `scheduleWriteback` pattern with a retried failure outcome and a `评审通过` event on success, never rolling back `待合并`.
  status: done
  dispatched: implementer subagent (opus) → adapter half only: `packages/forge-adapters/src/index.ts` + `get-pull-request.shared.test.ts` + new `mark-pull-request-ready.shared.test.ts` / `comment-on-pull-request.shared.test.ts` in the bundle-53 worktree, no commit; server half (record head fields, approve → mark ready writeback) stays with self after the adapter lands.
  result: Commit 2497051. Adapter half by the implementer worker: `PrStatus = { state, head_sha, draft, head_branch }` (GitHub/Gitea `head.sha`/`head.ref`, GitLab `sha`/`source_branch`; draft from GitHub `draft`, GitLab `draft`/`work_in_progress`, Gitea `WIP:` title), `markPullRequestReady` (GitHub REST `node_id` → GraphQL `markPullRequestReadyForReview`, GraphQL `errors` rejects; GitLab PUT title stripped of `Draft:`/`WIP:`/`[Draft]`/`(Draft)`; Gitea PATCH title stripped of `WIP:`; already-ready is a no-op), `commentOnPullRequest` (GitHub/Gitea issue comments, GitLab MR notes); `forgeGet`/`forgePost` now share `forgeRequest` so PUT/PATCH carry the #37 timeout; three shared specs 48 + 32 + 25 cases, all green against the three stub backends (worker measured 174/174 across seven adapter suites, package typecheck 0, eslint 0). Server half by self: `review.ts` `scheduleMarkReady` / `attemptMarkReady` / `retryPendingMarkReady` (`回写` `翻ready` ok/ambiguous/definite outcomes, same tracker as writeback so `settleWritebacks` drains it, retried on the poller tick, never rolls back `待合并`, summary comment behind `KAOLA_REVIEW_SUMMARY_COMMENT=1`), `poller.ts` `backfillSubmissionHead`; `submit_pr` records `head_sha` / `is_draft`. Covered by `review.test.ts` (Gitea WIP flip PATCH body asserted, 500 → ambiguous, 403 → definite supersedes, retry to ok, opt-in comment, backfill). `scripts/forge-smoke.ts` now walks Draft → blocking round → revision claim → follow-up push → `submit_revision` → approve → `getPullRequest().draft === false` → merge → `翻ready` 回写 + token scan (module loads, eslint 0; real run belongs to the validation mission).

- item: Phase 5 — dependent sub-tasks and restack: `parent_task_id` on publish/import (non-terminal parent only, no self/descendant/cycle → `400`/`409`), derived `repo.base_branch` for sub-tasks, claim gate `409 parent_not_ready`, approve gate `409 parent_not_completed`, restack automation when the parent reaches `已完成` (system message + `kind=restack` round; `待验收` child → `待修改`, other states message-only), `open_review_round` from a child Claim (`kind=downstream_finding`, `opened_by_task_id`; `403` when the Claim is not on a child of that parent), and a `system` notice on the children when the parent goes `已退回`/`已取消`; tests for each gate.
  status: done
  dispatched: self → `tasks.ts` (publish `parent_task_id`, derived `base_branch`, cancel notice), `claim.ts` (parent gate), `review.ts` (approve gate, `openReviewRound`, `restackChildren`, `notifyChildrenParentEnded`), `poller.ts` (restack/notice on terminal transitions); commit on `workflow/bundle-53`.
  result: Commit 2497051. `POST /api/v1/tasks` `parent_task_id` → `400 invalid_body` (missing parent) / `409 parent_invalid` (terminal parent or different repo; a new task cannot yet be its own ancestor); `deriveBaseBranch` = parent PR head branch while the parent is 待验收/待修改/待合并, parent base branch once 已完成, stored value before the parent submits; claim gate `409 parent_not_ready`; approve gate `409 parent_not_completed`; `restackChildren` on parent 已完成 (system blocking message + `restack` round, 待验收 child → 待修改, `restack` event `{ task_id, parent_task_id, round, base_branch }`); `open_review_round` from a child Claim (`downstream_finding`, `opened_by_task_id`, parent 待验收 → 待修改, otherwise append-only; `403` when the Claim is not on a child; `409 stale_claim` with no live Claim); `notifyChildrenParentEnded` on 已退回 (terminate / closed PR) and 已取消 (poster PATCH). All exercised in the `review.test.ts` sub-task case (parent → child claim gate → stacked base branch → downstream round → parent revision → child approve gate → parent merge → child restack with `base_branch: main` → child merge; parent2 terminate → child2 system notice, status unchanged).

- item: Phase 7 — SSE server: `GET /api/v1/stream` (session required; `401` for no session or `待批准`), broadcast at every write point (status transitions, `report_progress` → `progress`, review actions → `review_round`, discussion → `discussion_message`) with short bodies (`task_id` + summary, never `note` beyond the event contract and never a token), a 30s comment heartbeat, and `onClose` cleanup that closes every connection and leaves no dangling timer; tests for the auth gate, one `progress` event per heartbeat, and clean shutdown.
  status: done
  dispatched: implementer subagent (opus) → new `apps/server/src/stream.ts` (`registerStream`, `publishStreamEvent`, `closeAllStreams`) + `stream.test.ts`, `app.ts` registration only, in the bundle-53 worktree, no commit; the write-point wiring (`publishStreamEvent` calls) stays with self in Phase 2/3.
  result: Commit 2497051 (stream.ts / stream.test.ts / app.ts registration) plus write points in claim.ts (claim, release, submit_pr, progress), leases.ts is reached via task_updated from callers, poller.ts (terminal transitions), tasks.ts (poster PATCH), review.ts (rounds, messages, approve/withdraw/terminate, restack). Worker report: 17/17 (`stream.test.ts` + `events.test.ts`), server typecheck 0, eslint 0; cases: no-session 401, `待批准` 401, 200 `text/event-stream; charset=utf-8` + `: connected`, `progress` frame deep-equals payload with no `note`/`token`, `: ping` at an injectable interval, `app.close()` ends the stream and `openStreamCount()` returns to 0 with the process exiting on its own (no dangling timer), zero-connection publish is a no-op. Deviation recorded: the shutdown hook is `preClose` (measured: an `onClose` hook never runs because Fastify's own server-close hook blocks on the open SSE response). Independently re-run by self: 113/113 across stream + adapter suites.

- item: Phase 6 — Web (all Chinese): board columns `待修改` and `待合并`; cards showing `percent` / `phase` / round; task detail 「评审」 panel (PR link / `head_sha` / round / who holds the ball; per-round thread; message composer with `blocking`/`suggestion` kind and forge diff-link anchor parsing; buttons 提交本轮意见 / 通过 / 撤回通过 / 终止本次交付 hitting the six routes); publish wizard 「拆为子任务」 parent picker; `EventSource` subscription to `/api/v1/stream` with reconnect; `App.review.test.ts` and extended `App.board.test.ts`; `pnpm lint` green.
  status: done
  dispatched: implementer subagent (opus) → `apps/web/src/App.vue` + new `App.review.test.ts` + `App.board.test.ts` against the frozen `docs/api.md` #53 contract, in the bundle-53 worktree, no commit.
  result: Commit (feat(web): review panel …). Board renders the eight statuses with 待修改 / 待合并 after 待验收 (muted subtitles), cards show `第 N 轮`, `子任务 · 父 <id>`, and `percent%` + phase from SSE; task detail「评审」panel (PR link, `head_sha` 12 chars, round, 球在谁手里, messages grouped by round with the unrounded draft group, author/kind badges, anchor links, composer with kind select + anchor input, four action buttons with Chinese confirm on 终止) hitting `GET …/review`, `POST …/review/messages|rounds|approve|withdraw|terminate`; publish wizard「拆为子任务」adds `parent_task_id` only when chosen; `EventSource('/api/v1/stream')` handling `task_updated` / `progress` / `review_round` / `discussion_message` with a single 3s reconnect timer; `review-anchor.ts` `parseAnchorLink` for GitHub blob / GitLab `/-/blob/` / Gitea `src/commit` links (GitLab probe ordered first so a group named `blob` is not misread). Worker: 9 files 163 passed, typecheck 0, eslint 0, with mutation negative-controls on three assertions. Self re-run: 163/163, typecheck 0, eslint 0. api.md anchor typing corrected to all-optional keys to match `readAnchor`.

- item: Validate the frozen candidate: `pnpm test` / `lint` / `typecheck` / `build`, a repository-wide scan of new responses, fixtures, and docs for real tokens / hostnames / certificate identities, independent code and security review of the exact candidate bytes, real GitLab/Gitea forge smoke through `pnpm smoke:forge` where the provided streamer can drive it, and a `docs/smoke-test.md` record where browser / GitHub / human-merge / SSE-in-browser steps are marked 配合 unless actually executed; README / CHANGELOG / api / architecture docked to the implementation.
  status: done
  dispatched: self → gates (`pnpm lint` / `typecheck` / `test` / `build`) and secret scan in the bundle-53 worktree; real forge smoke `pnpm smoke:forge -- gitlab` then `-- gitea` with the local gitignored PAT file loaded into the process env only, log → scratchpad `/private/tmp/claude-501/-Volumes-WorkspaceA-ylminiserver-workspace-KaolaTasks/c41b8883-d3be-408b-8dd1-93239aa7e156/scratchpad/smoke-{gitlab,gitea}.log` (token-masked); code-reviewer subagent (opus) → `kaola-workflow/bundle-53/code-review.md` (main root) reviewing the exact candidate bytes of `origin/main..workflow/bundle-53`; security-reviewer subagent (opus) → `kaola-workflow/bundle-53/security-review.md`; record → `docs/smoke-test.md` 本轮记录 with 配合 items marked unexecuted; findings repaired inline and re-gated before close.
  result: HEAD 4875bd7. Gates at HEAD: `pnpm lint` 0; `pnpm typecheck` all five packages Done; `pnpm test` 1020/1020 node + 163/163 web; `pnpm build` all four packages Done. Secret scan of `origin/main...HEAD` product files: no tokens, private keys, IPs, or local paths (only synthetic fixtures). Real forge smoke (`pnpm smoke:forge -- gitlab` / `-- gitea`, PAT loaded from the main-root gitignored `.env` into the process env only, logs token-masked in the scratchpad): both exit 0 through Draft PR → blocking round → `待修改` → bridge revision claim (`review_round` 1) → `get_review_feedback` → follow-up push → `post_discussion_message(resolution)` → `submit_revision` → `待验收` → approve → `待合并` with `draft=false` and matching head on the forge → merge → `pollPendingReviews` → `已完成`, `回写` 认领/提交PR/完成/翻ready, `events.details` token-free — GitLab Issue #20 / MR !16, Gitea Issue #30 / PR #31 (first attempt Issue #19 / MR !15 and Issue #28 / PR #29 stopped at `待修改` on a smoke-script bug reading `rev-parse`, fixed in 9fb9099; those PRs remain open on the smoke repos, unmerged). Recorded in `docs/smoke-test.md` 本轮记录 with the 配合 items (browser review panel, SSE in a browser, wizard sub-task picker, GitHub GraphQL ready flip, human merge click) marked as not executed. Security review (`security-review.md`): PASS, three low notes all repaired in e5ddc3d (GET review gate for `待批准`, size caps on `body_md` / anchor / items / `phase`, 8 streams per user) with a pinning test. Code review (`code-review.md`): no blocker, four should-fix + four nits, all repaired in 4875bd7: reopen after `已退回` delivers a new PR (`liveSubmission` = `pr_state open`; terminate sets `terminated`; brief `review_round` 0 for an ended delivery), `submit_pr` optional `head_branch` so sub-tasks stack on webhook-managed instances without a poller fetch, `sweepExpiredLeases` publishes `task_updated`, `open_review_round` scans all sibling Claims, released Claim + new head → `stale_claim`, `notifyChildrenParentEnded` transactional, api.md `preClose` wording; each pinned in `review.test.ts` (13 cases). Docs docked: README, CHANGELOG, DESIGN v0.6, api.md, architecture.md, smoke-test.md. Not executed and honestly unclaimed: browser UAT of the Web panel / SSE / wizard, GitHub Draft→ready against a live repo (adapter covered by shared stub spec only), STABLE_PUBLIC_CA. Residual risk recorded from the security review: `head_sha` is the Agent's claim, verified by the poller only when null; an Agent pushing after `submit_revision` and before 「通过」 is not detected (out of #53 scope; DESIGN §17 records head_sha at submit). Smoke run predates the additive `head_branch` parameter (script re-linted and loads). Finalization filing: follow-up issue #54 (P3, enhancement, M3) for the unverified `head_sha` residual — confirmed present with a 917-byte body; duplicate probe `gh issue list --state all --search "head_sha 校验 forge"` → 1 hit (#53 itself).
