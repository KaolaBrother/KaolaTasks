gap: harness-flag-catch — waitForUatFlag empty catch swallows unexpected go content
gap: harness-bind — Path C --web defaults listen 0.0.0.0
gap: forge-timeout — GitLab waitForForgeHead getPullRequest 10s aborts the smoke
