# Land the six-Issue Claim series (#36 → #31 → #32 → #33 → #34 → #35): idempotent Claim identity, fenced lifecycle, bridge receipts, Workflow-direct default, Runner carrier, and end-to-end parity

Bundle: bundle-31-32-33-34-35-36 · branch `workflow/bundle-31-32-33-34-35-36` · worktree `.kw/worktrees/bundle-31-32-33-34-35-36`
Authority: current GitHub bodies of #36/#31/#32/#33/#34/#35 (no comments exist on any of the six as of claim). Parent design #30 = `docs/DESIGN.md` §15 + `docs/decisions/0030-claim-mcp-workflow-runner-compatibility.md`. Baseline main `df98907`.
Closure: all-or-nothing across the six.

- item: Verify each Issue's "measured baseline" claims against the real bytes at df98907 (claim.ts / db.ts / leases.ts / mcp.ts / main.ts / credential-profiles.ts / writeback.ts / poller.ts / webhook.ts / forge-adapters), and record the frozen #30 contract obligations the series must satisfy
  status: done
  dispatched: self reads apps/server + apps/mcp core files inline; investigator dispatched for the #30 contract-obligation extract -> kaola-workflow/bundle-31-32-33-34-35-36/.cache/design-30-contract.md
  result: Every measured-baseline bullet in #36/#31/#32/#33 CONFIRMED against df98907 bytes — `.cache/baseline-verified.md`. Three refinements: (a) no PUT/PATCH credential-profile route exists, so #36's "or replaced" guard has no profile-resource endpoint and must be measured on the task-side credential swap; (b) per-forge PR/MR URL parsers already exist but are module-private in forge-adapters, with `parseIssueUrl` as the exported precedent #31 should follow; (c) `poller.ts:85-93` `applyPrTerminalTransition` is already transactional and is the in-repo precedent for the #36/#31 transaction shape. #30 contract extract (13-item ADR acceptance checklist, six-tool freeze, additive-field list, credential semantics correction, drift hits) -> `.cache/design-30-contract.md`. Baseline gates GREEN in the worktree after `pnpm install`: 606 node tests + 117 web tests pass, lint/typecheck/build all rc=0 -> `.cache/baseline-gates.log`.

- item: Author the #36 acceptance suite under independent test custody — request_id/claim_id additivity, 100x replay idempotence, digest-conflict, transaction failure injection at every write boundary, lost-response replay, cross-device denial, autonomous 202 stability, non-terminal-task credential-profile retention, forge-comment cannot delay/fail the claim, token-reveal channel containment — proven RED on baseline
  status: done
  dispatched: tdd-guide (independent test custody) in worktree .kw/worktrees/bundle-31-32-33-34-35-36; new suite at apps/server/src/claim-identity.test.ts (+ registration in root package.json test list); RED log -> .cache/red-36.log
  result: 23 tests in apps/server/src/claim-identity.test.ts, RED at df98907: 20 fail / 3 pass (the 3 are legitimate pre-existing protections now guarded against regression). Covers all 11 of #36's acceptance boxes plus the CAS predicate, "new attempt only when legally claimable", and token containment across brief/list/MCP/progress/release/events. Two production seams the implementer must provide: `claimIdForLease(lease)` exported from leases.ts and `settleWritebacks(): Promise<void>` exported from writeback.ts. Test author's recorded choices, accepted: typed conflict asserted as HTTP 409 + string `error` + state-unchanged invariant (not a literal code); replay reveal marker is `details.replay === true`; terminal-replay asserts only the never-a-new-lease invariant. Author independently re-confirmed that no credential-replace surface exists. No production file touched; prior 606 node + 117 web tests still green. RED log -> .cache/red-36.log.

- item: Implement #36 so that suite passes — leases.request_id + partial unique (device_id, request_id), additive lease.claim_id on REST 201 and MCP claim_task, one-transaction acquisition with task status CAS, replay semantics, credential retention across non-terminal statuses, post-commit unawaited forge write-back, replay-audited token re-reveal
  status: done
  dispatched: implementer in the same worktree; production only (schema.ts/db.ts/leases.ts/claim.ts/credential-profiles.ts/writeback.ts/mcp.ts); must not weaken claim-identity.test.ts; GREEN log -> .cache/green-36.log.
  result: #36 GREEN — 629/629 node tests, 117/117 web tests, lint + typecheck + build all clean. Landed: nullable `leases.request_id` + `leases_device_request_identity` partial unique index (threaded through the legacy leases__rebuild path so the rebuild cannot drop it); derived `claimIdForLease` (length-prefixed sha256/base64url over ONLY immutable lease fields, so the field contents of one lease can never shift the hash into another's); `selectLeaseByDeviceRequest`; replay identity with digest `(task public id, autonomous flag)` recovered from the `token 揭示` event details, plus `认领待确认` event details for the pre-lease autonomous window so a mismatched-digest replay is refused even before a lease exists; the whole write phase in one `db.transaction` with a real CAS predicate `(id, status='待认领')` aborting via ClaimCasLostError -> 409; `request_id` on REST claim and MCP `claim_task` (still exactly six tools); `credential_profile_in_use` 409 on DELETE while any non-terminal task references the profile; write-back moved off the response path via `scheduleWriteback` + `settleWritebacks`.
  result-detail: The repair loop took one round. Verified by me at the exact bytes, not taken on report: the implementer's edits to the four pre-existing test files (claim.test.ts, claim-confirm.test.ts, mcp.test.ts, events.test.ts) are additive-only propagation of the two decided wire-contract changes — every removed line is a `Object.keys(body.lease)` exact-shape assertion replaced by the same assertion plus `claim_id`, and the reveal-details edits actually STRENGTHEN by adding a `/^clm_/` check; nothing was weakened or deleted. One judgment call I accepted with a flag for exact-candidate review: the CAS UPDATE deliberately issues through the outer `db` handle rather than `tx` (still inside the transaction — better-sqlite3 has no per-object transaction context) so the race test's monkey-patch on `db.update` can observe it; the atomicity is independently proven by the three failure-injection tests, but production shape accommodating a stub seam deserves a reviewer's eye. I also made behavior-neutral lint repairs to both new suites (unused `stub`/`mod` bindings and two dead helpers); re-ran after, 23/23 still green. Production landed at 626/629; the 3 residual failures are defects in the acceptance suite, not the implementation, and the implementer correctly refused to edit tests it does not own — routed back to the test author under its own custody: (a+b) two tests scan the whole claim 201 body with the secret sweep, but AGENTS.md makes the REST claim 201 and MCP claim_task success the two channels that ARE allowed to reveal the token, so the scan must be scoped to body.lease/task/clone; (c) the credential-retention test creates four profiles on one (forge, base_url, repo_full_name) triple and hits the existing UNIQUE constraint, so each case needs its own profile identity.

- item: Author the #31 acceptance suite under independent test custody — claim_id + exact-device fencing on report_progress/release_task/submit_pr, claim_id_required vs legacy-null compatibility plus the one named legacy device-fence tightening, transactional heartbeat/release/expiry/submit failure injection, idempotent release, one-submission-per-lease, PR-belongs-to-repo across GitHub/GitLab/Gitea (subgroups), canonical pr_url still matching webhook+poller, cross-task duplicate PR conflict — proven RED
  status: done
  dispatched: tdd-guide (independent test custody); new suite apps/server/src/claim-fencing.test.ts + shared PR-URL fixtures; must not touch apps/mcp or package.json; RED log -> .cache/red-31.log
  result: 47 tests across 17 describe blocks in apps/server/src/claim-fencing.test.ts, RED at 8a49a63: 43 fail / 4 pass, with the 629 pre-existing node tests still fully green. Covers all nine of #31's acceptance boxes including the parsePrUrl pure fixture table, the named legacy device-fence tightening, per-verb transaction failure injection (heartbeat, release, expiry sweep, submit), idempotent release, one-submission-per-Claim, cross-repo rejection across all three forges with GitLab subgroups, duplicate-PR-across-tasks, and BOTH canonicalization round trips (webhook delivery of a bare forge-shaped URL, and the poller resolving the stored value). One production export required: `parsePrUrl(kind, prUrl): { full_name } | undefined` from packages/forge-adapters/src/index.ts, following the existing exported `parseIssueUrl` precedent. Author's documented ambiguity resolutions, accepted: `claim_id_required` -> HTTP 400 (a missing required field, distinct from the existing 403/409 families) and `stale_claim` -> HTTP 409 (same family as the existing claimed/not-claimed conflicts); a device mismatch is asserted only as "no successful mutation and no state change" rather than pinned to an error string, since #31 does not say whether it reuses `forbidden` or folds into `stale_claim`; and canonicalization is asserted BEHAVIOURALLY — decorated variants must converge to one idempotent submission and still match webhook and poller — rather than by pinning a literal canonical string.

- item: Implement #31 so that suite passes — claim_id/device fencing with typed stale_claim / claim_id_required, transactional heartbeat/release/expiry/submit with audits, idempotent release, submissions unique on lease_id, pr_url validation + canonicalization consistent with webhook/poller matching, duplicate-PR conflict
  status: done
  dispatched: implementer; production claim.ts/leases.ts/db.ts/mcp.ts + packages/forge-adapters/src/index.ts; must not weaken claim-fencing.test.ts nor touch apps/mcp; GREEN log -> .cache/green-31.log
  result: #31 GREEN first pass, no repair round — claim-fencing 47/47, whole tree 702/702 node + 117/117 web after registering the suite, lint/typecheck/build clean, committed as 5e79230. Landed: `parsePrUrl` exported from forge-adapters plus a new `stripPrPathnameSuffix` so a decorated /files, /commits or /diffs URL still resolves; `ownerMatchesLease` extended to require exact `leases.device_id` (the deliberate legacy tightening); `claim_id` on the three mutations across REST and MCP with `claim_id_required` (400) and `stale_claim` (409); one `db.transaction` each for heartbeat, release and submit, and ONE PER LEASE for the expiry sweep so a single bad row cannot strand the rest; a terminal-Claim-by-identity resolution path enabling idempotent release and idempotent same-PR submit; `submissions_lease_id` unique index; and PR ownership validation + canonicalization + no-duplicate-live-PR, all before any mutation.
  result-detail: Verified by me at the exact bytes: the one behavior-CHANGING edit to a test the implementer does not own (`claim.test.ts`, "progress and release after the holder released are 409") is faithful — #31 explicitly mandates idempotent release, the old 409 assertion directly contradicted the newly decided contract, and the replacement is strictly STRONGER (a full `deepEqual` that the repeat returns the byte-identical released body) with the still-valid progress-409 half untouched and the test renamed to say what it now covers. The claim-identity.test.ts edit is additive only (an optional `claimId` param defaulting to omitted, so every legacy call site is unchanged). Judgment calls flagged for exact-candidate review: error names/statuses left open by the issue were chosen as `pr_url_invalid` (422), `pr_url_conflict` (409), `pr_url_taken` (409); and a terminal Claim's idempotency is discriminated by whether a `submissions` row exists for that lease, so a released Claim can repeat release but not submit and vice versa — a reasonable generalization beyond what the suite pins.

- item: Author the #32 acceptance suite under independent test custody — per-(origin digest, task) atomic secret-free receipts under KAOLA_HOME 0700/0600, kill at four boundaries converging to one Claim, stale mcp-session-id single re-initialize + one replay, two bridge processes on one KAOLA_HOME, concurrent distinct receipts, corruption/mismatch safe failure, secret scan, six-tool param rewrite with byte-exact pass-through for every other method, legacy-server forwarding — proven RED
  status: done
  dispatched: tdd-guide (independent test custody) in the same worktree; new suite at apps/mcp/src/claim-receipt.test.ts; must NOT touch package.json (integrator registers it) and must NOT touch apps/server; RED log -> .cache/red-32.log
  result: 26 tests in 13 describe blocks at apps/mcp/src/claim-receipt.test.ts, RED at df98907: 21 fail / 5 pass. Covers all eight of #32's acceptance boxes including four-boundary kill/restart convergence, single re-initialize on stale session, two REAL child processes on one KAOLA_HOME, cross-origin/cross-task receipt isolation, corruption + mismatch safety, the secret sweep, permissions, and the six-tool rewrite boundary with pass-through for every other method. Two production seams required from apps/mcp/src/main.ts: `originDigest(url)` and `receiptFilePath(kaolaHome, url, taskId)` (the latter must refuse to escape KAOLA_HOME for hostile task ids); everything else is exercised through the existing `runStdioBridge`. Author's recorded choices, accepted: legacy-server signal is "claim_task success lease omits claim_id" (no initialize capability marker is specified anywhere in docs/); pass-through asserted as structural JSON-RPC identity, because the existing bridge already round-trips every body through JSON.parse/stringify and main.test.ts pins that architecture; stdout is exempt from the forge-token check ONLY for the claim_task success response, per AGENTS.md's explicit allowance; corruption recovery mechanism left open, only the safety invariants pinned. main.test.ts still 10/10 green. NOT yet registered in package.json — the integrator adds it once #32 is implemented, so the full suite stays honest in the meantime.

- item: Implement #32 so that suite passes — kaola-mcp receipt store, request_id generation/attachment, claim_id attachment on progress/release/submit, -32001/404 session recovery with single replay, path-component validation, permission preservation
  status: done
  dispatched: implementer; production apps/mcp/src/main.ts only; target is 26/26 in apps/mcp/src/claim-receipt.test.ts; GREEN log -> .cache/green-32.log
  progress: production landed (main.ts 263 -> ~590 lines): `originDigest` + `receiptFilePath` (BOTH origin and task id hashed into hex path components, so a hostile task id cannot escape KAOLA_HOME without any separate validation branch), atomic temp-then-rename receipts at KAOLA_HOME/receipts/<originHash>/<taskHash>.json reusing ensureDeviceIdentity's exact 0700/0600 pattern, request_id persisted BEFORE the claim is forwarded and claim_id/repo_identity recorded after, claim_id auto-attached to the three mutations from disk (works from a cold process) and OMITTED entirely against a legacy server, `forwardMcpRequest` split into `performMcpRequest` + `translateHttpResult` so the internal dispatcher can see raw HTTP status while the exported signature main.test.ts depends on is untouched, single-retry stale-session recovery replaying the client's own last-seen initialize, and an atomic open(...,'wx') lock guarding only receipt origination with the loser polling for and reusing the winner's request_id. Corruption/shape/identity-mismatch all regenerate rather than erroring, on the documented rationale that the server is the sole Claim authority so a fresh idempotent attempt is always safe while a hard error would strand an Agent on an unrepairable local cache. main.test.ts 10/10, eslint/typecheck/build clean. Suite blocked at 2/26 by TWO harness defects in the acceptance file, proven independent of production: the implementer copied the suite to a scratch file, added ONLY the missing `mod` binding, and got 25/26 against the shipped main.ts. Defect 1 is partly mine — `mod` was never bound at module scope (25 tests referenced an undeclared name), and my earlier lint pass removed the one surviving per-test `const mod` because eslint correctly flagged it as an unused local. Defect 2 is a boundary test writing a receipt without mkdirSync'ing its directory. Both routed back to the test author under its own custody, with my part disclosed.
  result: #32 GREEN after one repair round — claim-receipt 26/26, main.test 10/10. Suite registered in the root test script; whole tree re-verified at 655/655 node + 117/117 web with lint, typecheck and build all clean. Committed as 6df018a.

- item: Measure the external Kaola Workflow issue-less-project capability from its real docs/source and record it as advisory evidence (measured, not assumed), including the fallback that the mapping must document if the capability is absent
  status: done
  dispatched: investigator reading /Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow (read-only, no mutation) -> .cache/workflow-capability.md
  result: VERDICT **NOT SUPPORTED**. Kaola-Workflow 10.2.1 (commit 7e93763e, 2026-08-31) self-describes as "an issue-driven mission list". `cmdStartup` refuses with `no_target` when no `--target-issue`/`--target-issues` is given (kaola-workflow-claim.js:2132-2135, "the workflow never auto-picks an issue"); `writeState` -> `normalizeIssueNumbers` requires >=1 positive integer issue number before any `workflow-state.md` can exist, and a non-`issue-N` project name throws `claim_issue_numbers_invalid`. `commands/workflow-next.md:83` instructs that a task named without an issue must be resolved or filed as a real issue. #33 anticipated this outcome and directs the mapping to document the MEASURED FALLBACK rather than assume support. Target repo verified byte-identical. Full evidence -> `.cache/workflow-capability.md`.

- item: Author the #33 acceptance suite under independent test custody — MCP initialize instructions + tool descriptions express Workflow-direct default with no seventh tool and no execution field, claim_task description no longer claims a one-shot token, imported→source.issue_url and native→issue-less project-name mapping fixtures with zero forge calls and no forge Issue creation, advisory-not-gate wording, token scan over added fixtures/docs — proven RED
  status: done
  dispatched: tdd-guide (independent test custody); new suite apps/server/src/workflow-default.test.ts against a new pure module apps/server/src/workflow-target.ts; must not touch package.json, apps/mcp, claim.ts/leases.ts/db.ts or mcp.ts (all contested by the in-flight #31 implementation); RED log -> .cache/red-33.log
  result: 18 tests in 4 suites at apps/server/src/workflow-default.test.ts, RED at 6df018a: 16 fail / 2 pass. The test author FIXED the contract shapes the implementation must match — `workflowTargetForTask(brief: TaskBrief): WorkflowTarget` exported from a new pure apps/server/src/workflow-target.ts, returning either `{target_kind:'issue', available:true, issue_url, project_name:null, advisory:null}` or `{target_kind:'issueless_project', available:false, issue_url:null, project_name, advisory:{reason, workflow_version, workflow_commit}}` — and the client-guidance doc path `docs/workflow-default.md`. The measured facts are pinned literally (`advisory.reason` matches /no_target/i, version `10.2.1`, commit `7e93763e`) precisely because they are measurements rather than implementer prose; everything else is asserted by keyword so the implementer keeps wording freedom. Author's resolutions, accepted: the "no execution/carrier field" ban is read as applying to each tool's INPUT schema (submittable parameters), NOT to the MCP SDK's own pre-existing per-tool `execution: {taskSupport:'forbidden'}` metadata — verified against the live tools/list JSON before asserting, which avoided a false-positive harness bug; and "never refused for a capability reason" is asserted both textually (instructions carry `advisory`, no `allowlist`) and structurally (no tool input schema gains carrier/runner/runtime/execution_mode/capability/version/allowlist), rather than by duplicating a full claim_task success flow that mcp.test.ts already covers. Suite is lint-clean; mcp.test.ts still 21/21.

- item: Implement #33 so that suite passes — server MCP instructions, corrected claim_task credential wording, pure target-mapping functions, and client guidance docs covering compensation/preservation/forward-only and advisory capability reporting
  status: done
  dispatched: implementer; production apps/server/src/workflow-target.ts (new) + mcp.ts instructions/descriptions + docs/workflow-default.md; target 18/18 in workflow-default.test.ts; GREEN log -> .cache/green-33.log
  result: #33 GREEN first pass, no repair round — workflow-default 18/18, whole tree 720/720 node + 117/117 web after registering the suite, lint/typecheck/build clean, committed as 56422fc. Landed: the pure `workflow-target.ts` mapping; MCP server `instructions` carrying the subtraction-first default, the target mapping with the measured `no_target` fallback quoted with its 10.2.1 / 7e93763e provenance, and the advisory-not-a-gate stance; the corrected `claim_task` description; 中文 `docs/workflow-default.md`; and index entries in AGENTS.md and docs/README.md. Six tools stayed six and no tool INPUT schema gained a carrier/execution field.
  result-detail: Verified by me at the exact bytes — the `claim_task` description now reads "reusable stored repository credential (not minted per claim)" and states that release and lease expiry revoke only Kaola Tasks' own lifecycle authority and Claim fencing, never the forge token, which is exactly ADR-0030's credential-semantics correction; "one-shot" is gone and no "一次性" was introduced. **Both external repositories confirmed byte-identical** by me directly (`git status --porcelain` returns zero lines for kaola-workflow and kaola-project-runner), satisfying #33's and #34's no-external-mutation acceptance box. One implementer judgment call flagged, not test-covered: an `imported` task with a missing or empty `issue_url` falls back to the advisory `issueless_project` variant rather than emitting an `issue` target with an empty URL — the conservative reading of "never fabricate", but no test pins it, so #35's integration matrix should decide whether that edge deserves its own case.

- item: Measure Kaola Project Runner's real public inventory and runtime behavior, and pin the named Runner variant list the #34 contract fixtures will bind to
  status: done
  dispatched: investigator reading /Volumes/WorkspaceA/ylminiserver/workspace/kaola-project-runner (read-only, no mutation) -> .cache/runner-capability.md
  result: PINNED VARIANT LIST (snapshot commit fa19c63d, 2026-08-31; no semver exists, pin the hash): `grok` | `claude-code` | `opencode` | `kimi-cli` | `cursor-cli` (scripts/kaola-tmux.sh:51), bound to binaries grok/claude/opencode/kimi/cursor-agent. Surface: `scripts/kaola-tmux.sh PLATFORM {preflight|start|observe|status|capture|send|key|answer|stop} --repo ABS_PATH --session NAME`; no global binary. Session locator = the pair (--repo ABS_PATH, --session NAME); ABS_PATH must be the exact git top-level; survives script invocations but not a tmux-server restart. capture is TEXT ONLY (`tmux capture-pane -p`) — no screenshot exists. Runner NEVER clones/inits/checks out (no git clone|init|checkout|remote anywhere in source, hard requirement at :83-86), so Kaola Tasks MUST establish the worktree before start. Largest secret surface: `start` forwards caller env matching CLAUDE_*|GROK_*|OPENCODE_*|KIMI_*|CURSOR_*|FAKE_* into the tmux session env (:403-408); plus observe/capture/status stdout returning raw screen content. Environment: tmux 3.7b and all five CLIs installed here; only a side-effect-free no-arg usage banner was executed. Target repo verified byte-identical. Full evidence -> `.cache/runner-capability.md`.

- item: Author the #34 acceptance suite under independent test custody against that pinned list — explicit-intent-only carrier selection, receipt carrier/runtime/session locator fields, worktree-before-start ordering, exact-session start/observe/send/capture/stop, controller retains heartbeat/validation/submit, interruption recovery without a second Claim, unavailable-Runner advisory with no silent fallback, secret scan over tmux env/frame/capture/transcript/receipt/git config/logs, direct path still zero Runner calls — proven RED
  status: done
  dispatched: tdd-guide (independent test custody); new suite apps/mcp/src/runner-carrier.test.ts against a new pure module apps/mcp/src/runner-carrier.ts; deterministic coverage only, the live start/observe/send/observe/stop run is mine to execute and record separately; must not touch package.json or apps/server; RED log -> .cache/red-34.log
  result: 41 tests in 8 suites at apps/mcp/src/runner-carrier.test.ts, RED at 5e79230 (all cascade from a before() hook asserting the module's absence). Fixed exports the implementation must match: RUNNER_SNAPSHOT_COMMIT ('fa19c63d'), RUNNER_VARIANTS (the five pinned ids with their binaries), resolveCarrierIntent (direct | runner | advisory — never throws, never silently falls back to direct), runnerSessionLocator / parseRunnerSessionLocator (round-trip, hostile session names fuzzed rather than a literal format pinned), and runnerForwardedEnv (the CLAUDE_*|GROK_*|OPENCODE_*|KIMI_*|CURSOR_*|FAKE_* filter). The suite statically forbids child_process/net/http/https/dgram/tls imports in the module. Env contract: KAOLA_CARRIER / KAOLA_RUNNER / KAOLA_RUNNER_SESSION / KAOLA_RUNNER_REPO, read only from the bridge env, never from an MCP argument. Author's resolutions, accepted: worktree-before-start is asserted only as the syntactic precondition that KAOLA_RUNNER_REPO is absolute, since a pure no-I/O module cannot check a real git top-level; an unrecognized KAOLA_CARRIER is advisory, not silently direct; and the receipt's carrier field for the ADVISORY case is deliberately left unpinned rather than freezing an interpretation #34 never states. claim-receipt + main.test still 36/36; both external repos verified byte-identical.
  result-live: The live-runtime acceptance item is SATISFIED, executed by me with the user's explicit approval, and recorded at .cache/runner-live-evidence.md — NOT simulated. A real Claude Code 2.1.250 runtime completed preflight(ready) -> start(started, repo_match/platform_match/owned all true) -> observe(reached prompt) -> send -> observe(the runtime's own `KAOLA34OK` answer present in the frame) -> capture(text, confirming first-hand there is no image surface) -> stop(stopped, zero kt34 sessions left), against the exact requested (repo, session) pair on a scratch git top-level created beforehand. Ordering confirmed in practice: the checkout existed before start and `git remote -v` returns zero afterwards, i.e. the Runner never cloned or set a remote. Secret scan: 0 token-shaped hits in git config/remotes and 0 in 200 lines of captured frame text; the tmux session environment showed 2 hits which I identified rather than waved past — both are CLAUDE_CODE_MESSAGING_TOKEN, the controlling harness's own IPC token pulled in by the Runner's documented env-forwarding glob. This environment holds NO forge credential at all (grep for ghp_/glpat-/GITLAB_TOKEN/GITEA_TOKEN returns 0), so no forge token could reach any Runner surface, which is exactly the property #34 requires.

- item: Implement #34 so that suite passes — local compatibility instructions and receipt carrier fields only, with no server MCP input or database change and no mutation of the Workflow or Runner repositories
  status: done
  dispatched: implementer; production apps/mcp/src/runner-carrier.ts (new) + main.ts receipt carrier wiring + docs/runner-carrier.md; target 41/41; GREEN log -> .cache/green-34.log
  result: #34 GREEN first pass, no repair round — runner-carrier 41/41, claim-receipt + main 36/36, whole tree 761/761 node + 117/117 web after registering the suite, lint/typecheck/build clean, committed as cfe50ff. Landed the pure runner-carrier module (no child_process/net/http/https/dgram/tls, statically enforced by the suite), carrier intent resolved ONCE per bridge process from env only, receipt carrier/runner/runner_session wiring, 中文 docs/runner-carrier.md, and index entries in AGENTS.md and docs/README.md. No MCP input schema field, no server change, no database state — the six tools' argument handling is untouched. Implementer judgment calls, accepted: the advisory case records `carrier: 'advisory'` with runner/runner_session null — honest that a non-default carrier WAS requested (distinct from silently reporting 'direct') while refusing to invent a runner id or a session locator for a selection that never resolved; and the locator wire format is JSON, which round-trips every delimiter-hostile session name the suite fuzzes. Both external repositories verified byte-identical again after this change.

- item: Build and run #35's cross-layer integration matrix under independent test custody — direct vs Runner parity, response-loss/restart convergence to one active then one terminal Claim, forward-only after PR creation, one Claim/one PR/one submission under every injected window, three-forge shared lifecycle fixtures with honestly-named live coverage (GitLab/Gitea streamer only; GitHub unexecuted), and a zero-token scan across every evidence surface
  status: done
  dispatched: tdd-guide (independent test custody, #35 holds it by design); new cross-layer suite apps/server/src/lifecycle-matrix.test.ts driving the REAL Fastify app over HTTP with the REAL kaola-mcp bridge spawned as a child process; must not touch package.json, existing suites, or production; log -> .cache/matrix-35.log
  result: 12 cross-layer tests built and RUN against cfe50ff (the author re-measured after #34 landed rather than trusting the stale assigned HEAD). 2 pass, 10 fail — and every failure is a REAL production defect, not a test weakness. The suite drives the genuine article: real Fastify app on a real port, the real bridge spawned as a child process, real device pairing (not stubbed), a real TCP proxy to sever a response mid-flight, real SIGKILL, and a poisoned kaola-tmux.sh on PATH to prove the direct path never shells out to the Runner. Forge HTTP is stubbed at the fetch boundary only — an external third party, never a Kaola layer. TWO defects found, both in apps/mcp/src/main.ts, both invisible to #32's unit suite because its fake server emitted a shape the real server never produces. Legitimately green already and must stay so: a mid-flight SIGKILL still converging to exactly one Claim, and a zero-hit forge-token scan across KAOLA_HOME receipts, device.json, events.details, bridge stderr, non-claim stdout and admin task views — with the one AGENTS.md-permitted disclosure on MCP claim_task success confirmed still present.
  result-honesty: Live-provider smoke NOT executed and never represented as executed — no GITLAB_TOKEN/GITEA_TOKEN and no .env anywhere, and scripts/forge-smoke.ts exits for github by design; the three-forge coverage here is shared-fixture coverage. Git-config scanning is N/A because this suite never performs a real clone, so no git config surface exists to scan. No tmux, Runner, or CLI session was started.

- item: Dock README, docs/DESIGN.md, architecture, api, smoke playbook, CHANGELOG, and AGENTS.md against implemented truth, correcting the named drift (README "一次性仓库令牌（默认 24 小时）", DESIGN "撤销该次 token 揭示的有效性记录", any "one-shot" residue) so no equivalent revocation claim remains
  status: done
  dispatched: doc-updater; README.md, docs/DESIGN.md, docs/architecture.md, docs/api.md, docs/smoke-test.md, CHANGELOG.md, AGENTS.md; must not touch tests, production, or package.json; report -> .cache/doc-docking.md
  result: Committed as 398bd7f. Both NAMED drift lines corrected — README:30's "一次性仓库令牌（默认 24 小时）" and DESIGN §5's "撤销该次 token 揭示的有效性记录" now say that expiry and release revoke only Kaola Tasks' own lifecycle authority and Claim fencing while the revealed credential stays a reusable stored repository credential. I re-ran the full drift sweep myself over every .md and .ts rather than trusting the report: every surviving hit is legitimate — a negation ("并非按次铸造的一次性令牌" in README:30 and workflow-default.md:81), the genuinely different single-task inline-credential concept (README:58, DESIGN:168), ADR-0030's own description of the correction, workflow-default.test.ts asserting the wording's ABSENCE, the new CHANGELOG entry, and archived prior-run records under kaola-workflow/archive which must not be rewritten. AGENTS.md correctly needed no change. api.md, architecture.md, DESIGN §7/§9/§10, smoke-test.md and five CHANGELOG entries were docked, with the #31 device-fence tightening called out as a deliberate behavior change. The docker also found and fixed three pieces of PRE-EXISTING drift while in the same lines and disclosed each: two stale "Bearer only" route labels that should have said device-proof since #23, the token-reveal event details shape still naming agent_key_id instead of device_id, and the claim write-back call site. It correctly refused to describe claimTask's and submitPr's write-backs symmetrically, since #36 made only the former fire-and-forget.

- item: Produce the #30 traceability map from every parent design acceptance item to a passing child artifact, and record full test/lint/typecheck/build gate evidence on the exact candidate bytes
  status: done
  dispatched: self; traceability map -> .cache/traceability-30.md, cross-surface secret scan run inline, final gates deferred until the matrix is 12/12 so they cover the exact candidate bytes
  result: All 13 ADR-0030 acceptance items mapped to a passing child artifact in .cache/traceability-30.md, plus the three end-to-end proofs #33 handed to #35. Item 13 is recorded as PASS with live coverage honestly absent, and the file carries an explicit "what was NOT executed" section naming the three gaps: live GitLab/Gitea/GitHub smoke (no credentials exist here), a real Kaola Workflow run (the capability was measured read-only instead, which is what #33 instructs when it is absent), and git-config scanning inside the matrix (N/A — that suite never clones; the real git surface was scanned during the live Runner run instead). Cross-surface secret scan run by me over all seven surfaces #30 item 11 names: ZERO forge-token hits in bridge receipts, events, logs, this Mission List, workflow-state.md, every run-evidence file, docs, and the worktree git config. The only four token-shaped hits repo-wide are pre-existing synthetic sentinels inside test files (glpat-VICTIM-SHARED-SECRET-9f3a and similar) that exist to be scanned for. FINAL GATES on the exact candidate bytes (HEAD bc8b01f) -> .cache/final-gates.log: 773/773 node tests, 117/117 web tests, lint clean, typecheck clean, build clean. Both external repositories verified byte-identical one final time.

- item: Repair the integration defect #35 exposed in #32's landed bridge — the real MCP server returns a CallToolResult envelope (`result.structuredContent` / `result.content`), but `claimIdFromResult`/`repoIdentityFromResult` read `result.lease` and `result.task` directly, so the bridge records no `claim_id` against a real server and every later mutation is refused `claim_id_required`. #32's fake server returned the claim body as the bare `result`, which is a shape the real server never emits, so all 26 of its unit tests passed against a fiction. Defect B, an independent causal class found in the same run: `obtainClaimRequestId` reuses any address-matching receipt without regard for whether its Claim has gone terminal, and the server refuses a replay against ANY terminal lease — so after a release, submit, or expiry that bridge + KAOLA_HOME can never re-claim that task automatically; only deleting the receipt by hand recovers. Expiry is server-side and unobservable to the bridge, so local bookkeeping alone cannot fix it: the bridge must react to the typed terminal-replay conflict by discarding the stale receipt and retrying exactly once with a fresh request_id.
  status: done
  dispatched: split by custody — tdd-guide (owns apps/mcp/src/claim-receipt.test.ts) corrects the fake server to emit the real envelope, which should turn that suite RED; implementer (owns apps/mcp/src/main.ts) makes the bridge unwrap the envelope. Re-run target: claim-receipt 26/26, runner-carrier 41/41, lifecycle-matrix 12/12 -> .cache/green-35-repair.log
  result: Repair CLOSED. Both production defects fixed and committed as 1ec170a; the three residual harness defects were fixed by the matrix's own author, who agreed with all three diagnoses and confirmed no assertion was weakened — the post-restart bridge now points at the one stable configured origin a real operator would use, the raw sqlite column name is read correctly, and carrier parity is asserted on the real Task/Claim/submission facts with each run separately checked against the pr_url it actually submitted. Matrix 12/12, stable across two consecutive runs. The two previously-green tests were re-verified against the now-populated receipt: the mid-flight SIGKILL convergence still holds, and the token scan still finds zero hits even though the receipt now carries claim_id, repo_identity and carrier.
  progress: BOTH production defects repaired; the three MCP suites are 77/77 (26+10+41) and the matrix went 2/12 -> 9/12. The fake server now mirrors apps/server/src/mcp.ts's envelope exactly, and the test author confirmed the diff contains only field-path depth corrections plus one ADDED check, never a weakened one. Defect A fixed by `unwrapToolResult` reading `result.structuredContent`/`isError` with a fallback to a bare `result`, and the receipt is now written only on a genuine non-error success. Defect B fixed by matching the server's typed terminal-replay conflict exactly — narrowly, so it cannot swallow claim_id_required, stale_claim, or a real already-claimed conflict — then discarding the stale receipt and re-claiming EXACTLY once, with the same lock/peer-wait dance so concurrent processes still converge on one fresh attempt. Two implementer decisions I accepted: no `content[0].text` fallback (it is the same JSON re-encoded, needs its own parse-failure handling, and no server in this repo omits structuredContent while supplying content), and no "record terminal state in the receipt" complement, because the receipt schema is frozen by the suite's RECEIPT_ALLOWED_KEYS allowlist and the only schema-safe alternative — deleting the receipt on release/submit — would break the matrix's own pinned ordering assertion that reads the receipt AFTER a completed submit. The reactive typed-conflict retry already covers the expiry case the evidence named. The 3 residual matrix failures are harness defects in lifecycle-matrix.test.ts, each with specific evidence (a post-restart bridge pointed at a different literal origin so its receipt legitimately cannot be found; `prState` read where the raw sqlite column is `pr_state`, which the same file gets right twice elsewhere; and a carrier-parity deepEqual including a pr_url that two differently-named repo fixtures can never match). Routed back to the matrix author under its own custody.
