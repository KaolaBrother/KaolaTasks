# issue-54 — 评审循环：核对 submit_revision / submit_pr 的 head_sha 与 forge 当前头，「通过」锚定到具体 commit

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/KaolaTasks/.kw/worktrees/issue-54` · branch `workflow/issue-54` · base `1897007`.

Design decisions fixed for this run (issue remedy was non-binding):
- `submissions` gains nullable `forge_head_sha` / `forge_head_seen_at`; the poller writes the forge head on every tick (in addition to the existing NULL-only backfill). `submit_pr` rows start NULL; `submit_revision` resets both to NULL when it records a new head.
- `GET …/review` exposes `forge_head_sha`, `forge_head_seen_at`, `head_stale` (true only when both `head_sha` and `forge_head_sha` are known and differ). The Web review panel shows a「forge 头已变化」notice when `head_stale`.
- `POST …/review/approve` performs a live `getPullRequest` (bounded timeout, same as 翻ready) before the transition: recorded `head_sha` NULL → backfill from forge and proceed; equal → proceed; differ → `409 head_sha_stale { error, message, recorded_head_sha, forge_head_sha }` and the observation is persisted; forge unreachable → compare against the stored `forge_head_sha` if any (differ → 409), otherwise proceed fail-open with `head_verified: false`. Response `{ task, round, head_sha, head_verified }`; `评审通过` event details gain `head_sha`, `head_verified`. Reviewer's remedy on 409: post a blocking message + 提交本轮意见 so the Agent hands the new head back via `submit_revision`.
- `submit_revision` / `submit_pr` keep the response path forge-free (documented trade-off); the poller's next tick records the forge head.

## 1
item: Update the contract first — `docs/DESIGN.md` (§9 tool table notes, §17 review loop, schema/events tables) and `docs/api.md` (review GET view fields, approve 409 `head_sha_stale` + `head_verified`, poller forge-head recording, schema columns, event details) to the decisions above.
status: done
dispatched: self (inline edits to docs/DESIGN.md and docs/api.md in the worktree)
result: `docs/DESIGN.md` v0.7 header, §9 `submit_revision` note, §10 submissions/events rows, §17.2 route table + 评审通过 details, §17.5 write points, §17.6 invariant, new §17.7; `docs/api.md` review intro, GET view shape, approve head-check contract, SSE `task_updated` note, poller #54 paragraph, submissions #54 columns, events bullet. Committed on `workflow/issue-54` (docs-only commit).

## 2
item: Author failing acceptance tests for the new behaviour — server: poller records forge head each tick; review view `forge_head_sha` / `head_stale`; approve live-check 409 `head_sha_stale`, backfill-when-null, fail-open `head_verified:false` with stored-observation fallback; `submit_revision` resets the forge head; db-migration adds the two columns idempotently; web: panel shows the 「forge 头已变化」notice and maps `head_sha_stale` to a Chinese message. Prove they fail on base `1897007`.
status: done
dispatched: tdd-guide (sonnet) → tests in `apps/server/src/review.test.ts` (new `describe('issue #54 …')`), `apps/server/src/poller.test.ts`, `apps/server/src/db-migration.test.ts`, `apps/web/src/App.review.test.ts`; baseline-fail evidence to `/private/tmp/claude-501/-Volumes-WorkspaceA-ylminiserver-workspace-KaolaTasks/cb2f8039-f755-4fc5-ad76-0e2be791435b/scratchpad/issue-54-tests-baseline.md`
result: 15 tests added (db-migration ×3, poller ×3 incl. a real SSE socket, review ×7 covering (a)–(f) + view exposure, web ×2); server run 48 tests / 35 pass / 13 fail, all 13 new; web 165 / 163 pass / 2 fail, both new. Committed as the `test:` commit on `workflow/issue-54`. Flagged for mission 3: two pre-existing #53 tests need mechanical fixture sync (评审通过 deepEqual gains head_sha/head_verified; sub-task test's PR stub head must be advanced before the parent approve). Report: scratchpad `issue-54-tests-baseline.md`.

## 3
item: Implement server + web to make the mission-2 tests pass without weakening them — `schema.ts` + `db.ts` migration columns, `poller.ts` forge-head recording (+ `task_updated` stream publish when the observed forge head changes), `review.ts` view fields / async approve live check / event details / `submit_revision` reset, `mcp.ts` only if a surface shape changes, `App.vue` notice + error mapping. `pnpm lint`, `pnpm typecheck`, `pnpm test` green.
status: done
dispatched: implementer (sonnet) → production edits in the worktree (`schema.ts`, `db.ts`, `poller.ts`, `review.ts`, `App.vue`, plus the two mechanical #53 fixture syncs in `review.test.ts` explicitly authorized); report to `/private/tmp/claude-501/-Volumes-WorkspaceA-ylminiserver-workspace-KaolaTasks/cb2f8039-f755-4fc5-ad76-0e2be791435b/scratchpad/issue-54-impl-report.md`
result: `schema.ts` / `db.ts` (two idempotent ALTERs), `poller.ts` (`recordForgeHeadObservation` + `task_updated` on change), `review.ts` (view fields, async `reviewerApprove` with `checkHeadAnchor`, `submitRevision` reset), `App.vue` (notice + `head_sha_stale` map), two #53 fixture syncs (parent + child stub heads; event deepEqual). Main fixed one edge: fail-open with nothing recorded/observed returned `''` and would have written an empty head — now `null`. Verified by main: `pnpm lint` 0, `pnpm typecheck` 0, `pnpm test` 0 (web 165/165; server node --test counts in scratchpad `issue-54-test-run.txt`). Committed as the `feat:` commit. Report: scratchpad `issue-54-impl-report.md`.

## 4
item: Extend `scripts/forge-smoke.ts` with a drift leg — after `submit_revision`, push one more commit on the same branch, expect `approve` → `409 head_sha_stale`, then reviewer blocking message + round → Agent re-claims and `submit_revision(new sha)` → `approve` → `200` with `head_verified: true`; run it against GitLab and Gitea only if the local gitignored PATs are present, and record the outcome (or the fact it was not executed) in `docs/smoke-test.md`.
status: done
dispatched: self — edit `scripts/forge-smoke.ts` now (disjoint from missions 2/3), run `pnpm smoke:forge -- gitlab` / `gitea` with the local `.env` after mission 3; outcome lands in `docs/smoke-test.md` and this result line
result: Drift leg added to `scripts/forge-smoke.ts` (third push after `submit_revision` → wait until the forge reports it → `approve` 409 `head_sha_stale` with both shas → view `head_stale` → blocking + round → second revision claim → `submit_revision(new head)` → `approve` 200 `head_verified: true` → merge → 已完成). Real runs, all exit 0, no token in logs: GitLab Issue #22 → MR !18 `已完成`; Gitea Issue #34 → PR #35 `已完成`. First GitLab attempt (Issue #21 / MR !17) exposed GitLab's async MR `sha` refresh: approve right after the push saw the old head and passed — server correct against what the forge reported; smoke now waits for the forge, lag documented in DESIGN §17.7 and `docs/smoke-test.md`. Logs: scratchpad `issue-54-smoke-{gitlab,gitea}{,-2}.txt`. Docs committed.

## 5
item: Independent review of the exact candidate diff — code review (correctness, regression, scope, coverage) and security review (token leakage in the new fields/events/409 body, fail-open posture, forge round-trip on a session route); repair any finding inside this run and re-run lint/typecheck/test after any change.
status: done
dispatched: code-reviewer (opus) → `/private/tmp/claude-501/-Volumes-WorkspaceA-ylminiserver-workspace-KaolaTasks/cb2f8039-f755-4fc5-ad76-0e2be791435b/scratchpad/issue-54-code-review.md`; security-reviewer (opus) → `…/scratchpad/issue-54-security-review.md`; both reviewing `git diff 1897007..6896f9a` in the worktree, read-only
result: Code review: 1 blocking (R1: async approve made the 待验收 guard a check-then-act race — concurrent approve → 500 from the review_rounds unique index; a poller 已退回 during the forge round-trip overwritten; secondary: fallback compared a stale forge_head_sha snapshot), nothing at should-fix/nit, decision table / fixtures / token hygiene / migration / smoke leg verified clean. Security review: same R1 as medium; token leakage, bounded round-trip + authorization, fail-open auditability, Agent influence on new columns, SSRF/injection/DoS all clean. Repaired in `c7de3fc`: transaction re-reads task + submission and refuses 409 illegal_transition unless still 待验收 with an open submission (fresh row drives the round number); fallback re-reads stored forge head; two regression tests (interleaved terminal transition proven 200→409 on unfixed code; concurrent approve 200+409); api.md documents the re-check. Re-gated: lint 0, typecheck 0, test 0 (server 1035/1035, web 165/165). Post-repair smoke reruns PASS: GitLab Issue #23 → MR !19, Gitea Issue #36 → PR #37, no token in logs; recorded in docs/smoke-test.md. Reports: scratchpad `issue-54-code-review.md`, `issue-54-security-review.md`.
