# Issue #7 — Task CRUD + 发布即校验: tasks table, task REST surface, post-time validateToken, Chinese posting form

- item: Map the ground truth the implementation must fit — the register*(app, db) route conventions in apps/server/src/{auth,agent-keys,credential-profiles}.ts, how db.ts creates tables, the vault encryptToken/decryptToken signatures, the exact validateToken signature and its structured `missing` shape (读|推|PR) in packages/forge-adapters, and how @kaola/shared taskBriefSchema/parseTaskBrief/transitionTaskStatus map field-by-field onto DESIGN §6. Facts already known: no tasks table exists; bundle-4-5 deferred tasks.inline_token_encrypted to this issue; events table exists with (type, actor_user_id, created_at, details).
  status: done
  dispatched: code-explorer over the worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7; output to land at kaola-workflow/issue-7/.cache/ground-truth.md
  result: kaola-workflow/issue-7/.cache/ground-truth.md (~700 lines). Key facts, orchestrator-verified where they contradicted docs: (1) validateToken is NOT a standalone export — packages/forge-adapters/src/index.ts:67 has no `export`; only createForgeAdapter(kind, options).validateToken(cred, repo) is public. CLAUDE.md's snapshot line is wrong and must be corrected in item 6. (2) No zod / no Fastify schema at the HTTP boundary; bodies hand-parsed from unknown, snake_case on wire, camelCase internally. (3) Tables created by raw sqlite.exec(CREATE TABLE IF NOT EXISTS) in createDb — no migrations; a new table needs schema.ts + DDL const + exec line + drizzle schema map. (4) apps/server depends on neither @kaola/shared nor @kaola/forge-adapters, and NO workspace-to-workspace dep exists anywhere in the repo (verified) — #7 introduces the first. (5) validateToken has no try/catch and no timeout: network failure REJECTS rather than degrading, and the 读 capability only appears via the two all-missing early returns. (6) Test files must be appended to the root package.json test script or they silently do not run; idiom is set process.env then `await import('./app.ts')`; the existing fetch stub returns 500 for unrecognised Authorization and will collide with 发布即校验 tests. (7) The worktree has no node_modules (verified) — pnpm install must run there first.

- item: Update the DESIGN.md §6 contract FIRST, before any schema code — widen `credential` from a bare profile reference to a documented union of `{ profile_id }` or `{ inline: true }`, naming the second form §6 already gestures at with "或单任务临时 token 的引用" but never keyed. The token itself must still never appear in the brief. USER DECISION, taken in conversation 2026-08-21: union accepted; the three pinned tests in packages/shared asserting a `token` key inside `credential` throws must keep passing. Also record the §6 id form as `kt-YYYY-NNNN`. Docs-only edit; no code in this item.
  status: done
  dispatched: self (orchestrator owns the contract); edit lands in the worktree at .kw/worktrees/issue-7/docs/DESIGN.md §6
  result: docs/DESIGN.md §6 in the worktree, +12/-1. Added an **`id` 形式** paragraph fixing `kt-<年份>-<四位序号>` with an internal autoincrement PK not exposed, and a **`credential` 是引用，不是 token 本身** paragraph with a two-row table naming `{ profile_id }` and `{ inline: true }`, stating both forms carry no plaintext token and both reveal only on claim_task. The §6 JSON example still uses the profile_id form, so it continues to parse unchanged.

- item: Author the failing test suite for both surfaces in custody separate from the implementation — (a) packages/shared: the widened `credential` union parses both forms, still rejects any `token` key, and the DESIGN §6 example still parses; (b) apps/server/src/tasks.test.ts: tasks table per DESIGN §10, CRUD plus cancel and reopen, 发布即校验 gating with distinct Chinese errors for "token 权限不足" (structured missing 读|推|PR) versus "forge 不可达" (validateToken REJECTS on network failure — it has no try/catch and no timeout), both credential paths, and the invariant that no task response ever contains a token. Facts: no zod at the HTTP boundary today, bodies are hand-parsed from unknown with snake_case on the wire; the existing session stub returns 500 for unrecognised Authorization and will collide with fetch interception, so the suite needs its own seam. Append the new file to the root package.json test script or it silently will not run.
  status: done
  dispatched: two tdd-guide agents concurrently on disjoint files, both in the worktree .kw/worktrees/issue-7 — agent `tests-shared` extends packages/shared/src/index.test.ts for the credential union; agent `tests-server` creates apps/server/src/tasks.test.ts for the tasks HTTP surface. Each also writes a short handoff note to kaola-workflow/issue-7/.cache/tests-shared.md and .cache/tests-server.md respectively.
  partial: tests-shared RETURNED and orchestrator-verified. packages/shared/src/index.test.ts 87 -> 112 tests, 109 pass / 3 fail at RED baseline; I re-ran it myself and saw the same 3 failures with ZodError `Unrecognized key: "inline"`, and confirmed `git diff packages/shared/src/index.ts` is empty so custody held. Only 3 of the 25 new tests are RED — the other 22 pass today because strictObject already rejects unknown keys; they are guards that go red if the implementer uses z.object instead of z.strictObject or z.boolean() instead of z.literal(true). Suggested shape, verified standalone against zod 4.4.3: `credential: z.union([z.strictObject({ profile_id: z.string() }), z.strictObject({ inline: z.literal(true) })])` — z.union NOT z.discriminatedUnion (no shared discriminator), both members strict. Consequence to watch: the exported TaskBrief type becomes a union, so any unguarded `brief.credential.profile_id` read stops typechecking. Test-author rulings, all reasonable and accepted: `{inline:false}` rejected (marker, only true is meaningful); `二选一` read as exclusive so `{profile_id, inline:true}` throws; empty-string profile_id left legal since DESIGN constrains only `id`. Note: tests-shared is still awaiting tests-server before this item can close.
  handoff: kaola-workflow/issue-7/.cache/tests-shared.md
  result: BOTH test agents returned; orchestrator re-ran everything rather than trusting the counts. Full `CI=true pnpm test` in the worktree: tests 233 / suites 39 / pass 174 / fail 59 — the 59 failures are exactly tests-server's new suite and every pre-existing suite still passes. apps/server/src/tasks.test.ts alone: 59 tests, 0 pass, 59 fail, so nothing passes before implementation. tests-server touched only tasks.test.ts and the one-line root package.json test-script append; custody held. Every 401/404 test asserts the body (error === 'unauthorized' / 'not_found') as well as the status, so Fastify's own {"error":"Not Found"} cannot make them pass by accident. It solved the fetch-stub collision with a router keyed on bare token identity rather than URL — beginFetch(t) returning {oauth, forge} maps, stubbedToken() extracting from Authorization: Bearer|token and PRIVATE-TOKEN, unregistered tokens getting a loud 500, and task repos placed on a different origin (gitea.forge.example.test) from the Gitea OAuth provider; it verified that extractor against the exact header objects auth.ts and all three adapter branches emit (10/10) and verified its repo payloads against the REAL adapter (FULL->[], READ_ONLY->["推","PR"], NO_PR->["PR"], 401->all three, repo 404->all three, unreachable->rejects TypeError). It also empirically settled a point ground-truth had left UNVERIFIED: z.iso.datetime({offset:true}) DOES accept the bare-Z form, so new Date(sec*1000).toISOString() parses.
  handoff-spec: kaola-workflow/issue-7/.cache/tests-server.md — 59 test names, full request/response shapes, and 16 numbered judgement calls the implementer must respect rather than silently diverge from. Baseline capture: .cache/tests-server-baseline.txt

- item: Implement the packages/shared credential union only — widen taskBriefSchema.credential so `{ inline: true }` parses alongside `{ profile_id }`. Disjoint from all server work (different package, different file), so it runs now rather than waiting on the server tests. Verified suggestion from the test author, checked standalone against zod 4.4.3: z.union of two z.strictObject members, z.literal(true) not z.boolean, z.union not z.discriminatedUnion. Exactly 3 of the 25 new tests are RED; the other 22 are guards that must stay green.
  status: done
  dispatched: implementer agent `impl-shared`, worktree .kw/worktrees/issue-7, writing packages/shared/src/index.ts only; green proof to land at kaola-workflow/issue-7/.cache/impl-shared.md
  result: GREEN, orchestrator-verified by re-run — packages/shared/src/index.test.ts 112 tests / 112 pass / 0 fail. Used exactly the suggested construct: credential: z.union([z.strictObject({profile_id: z.string()}), z.strictObject({inline: z.literal(true)})]) with a comment citing DESIGN §6. Nothing beyond the credential field needed changing: parseTaskBrief is a bare taskBriefSchema.parse passthrough, transitionTaskStatus never touches the brief, and a repo-wide grep found no source importer of taskBriefSchema/TaskBrief outside packages/shared/src, so widening TaskBrief to a union broke no call site. tsc --noEmit and eslint both exit 0. Both union members being strictObject is what keeps the two new inline-form token tests ({inline:true, token} and {inline:true, token_ciphertext}) red-then-green — z.object would have key-stripped them into passing silently. Raised no findings against the test suite. Proof: kaola-workflow/issue-7/.cache/impl-shared.md

- item: Implement the apps/server tasks surface against the tests-server suite — schema.ts `tasks` table with INTEGER PK plus `public_id TEXT UNIQUE` in `kt-YYYY-NNNN` form and nullable `credential_profile_id` / `inline_token_encrypted` where exactly one is non-null; db.ts DDL const, exec line, and drizzle schema map entry; a `registerTasks(app, db)` module wired into app.ts addressing tasks by public_id on the wire (NOT parsePositiveInt, which 404s on non-numeric); post-time validation via `createForgeAdapter(kind, options).validateToken(cred, repo)` wrapped in its own try/catch and timeout, with distinct Chinese errors for 权限不足 versus 不可达; inline tokens through the AES-256-GCM vault; `状态迁移` event rows per the DESIGN §10 taxonomy. USER DECISIONS: integer PK + public_id; credential union. Workspace deps are already declared and PROVEN to work at both typecheck and --experimental-strip-types runtime, so cross-package imports are safe. Blocked only on the tests-server handoff note, which is the specification.
  status: done
  dispatched: implementer agent `impl-server`, worktree .kw/worktrees/issue-7, writing apps/server/src/{tasks.ts,schema.ts,db.ts,app.ts} against the 59 RED tests; green proof to land at kaola-workflow/issue-7/.cache/impl-server.md. Orchestrator ruling filling the one gap the spec left open (its judgement call 8, public_id counter policy): per-year sequence, zero-padded to 4 — next number within the current year, with the UNIQUE constraint as the backstop and a bounded retry on collision. Judgement call 14 (task repo vs profile repo mismatch) stays deliberately unimplemented and is recorded as a follow-up for the issue rather than invented here.
  result: ALL FOUR GATES GREEN, each re-run by the orchestrator rather than taken on report. CI=true pnpm test -> tests 233 / suites 39 / pass 233 / fail 0 (baseline was 174/59); typecheck, lint, build all exit 0. Implementer ran the suite 3x consecutively with identical results, so no ordering dependence. Files: apps/server/src/schema.ts (+45 tasks table + Task/NewTask types), db.ts (+34 TASKS_DDL + sqlite.exec + drizzle schema map), tasks.ts (new, 517 lines, registerTasks(app, db)), app.ts (+2, wired after registerCredentialProfiles). Custody held — git status confirms no test file and no root package.json touched by the implementer. Spot-checked the two constraints the tests demand as REAL SQL rather than triggers: db.ts:44 `public_id TEXT NOT NULL UNIQUE` and db.ts:66 `CHECK ((credential_profile_id IS NULL) != (inline_token_encrypted IS NULL))`. It reported NO test as wrong and worked around nothing. All 16 judgement calls respected, including 14 left unimplemented.
  accepted beyond-suite change: public_id sequence lookup orders by CAST(substr(public_id, N) AS INTEGER) DESC rather than lexically (tasks.ts:322). Both orderings pass all 59 tests, so the suite does not pin it — but the implementer MEASURED the failure: with kt-2026-9999 and kt-2026-10000 both present, lexical DESC returns 9999, so the 10001st task of a year re-proposes a taken id, exhausts the bounded retries, and 500s. Kept, because it removes a real cliff for one expression and arrived with evidence rather than a hunch.
  notable interpretations recorded in the proof: present-but-wrong-typed optional fields 400 rather than silently defaulting; repo.base_url required; PATCH gates active+full before the poster check; the inline token is encrypted BEFORE 发布即校验 so a vault failure surfaces ahead of a token verdict; public_id year uses getUTCFullYear so it can never disagree with the UTC-rendered created_at.
  proof: kaola-workflow/issue-7/.cache/impl-server.md

- item: Probe, before the implementer commits to the approach, whether apps/server can take @kaola/shared and @kaola/forge-adapters as workspace deps and still pass `tsc --noEmit` — no workspace-to-workspace dependency exists anywhere in this repo yet and both packages' exports point at raw .ts, so this is unproven and a failure reshapes item 4.
  status: done
  dispatched: self, in the worktree .kw/worktrees/issue-7
  result: RESOLVED — the first workspace-to-workspace dependency in this repo works on both halves. apps/server/package.json now declares @kaola/shared and @kaola/forge-adapters as `workspace:*` (CORRECTED ATTRIBUTION: added by the ORCHESTRATOR during this probe, not by tests-server as first recorded — the probe shell's cwd was already inside the worktree, and the `git checkout -- apps/server/package.json` intended to revert it ran against the MAIN root, which had never been modified. tests-server touched only tasks.test.ts and root package.json, exactly as instructed. The change is wanted by the server-implementation item, so it stays; pnpm-lock.yaml +6 lines). Declaring alone proves nothing, so I probed a REAL cross-package import via a throwaway apps/server/src/__probe.ts importing parseTaskBrief and createForgeAdapter: `tsc --noEmit -p apps/server/tsconfig.json` exits 0 — the feared TS6059 "not under rootDir" does NOT fire, because resolution goes through node_modules symlinks and noEmit means rootDir is not enforced for emit. Runtime confirmed separately: `node --experimental-strip-types` importing that module printed OK and exited 0, which matters because that is how the server actually starts. Both probe files were deleted; only the deps, DESIGN.md, and the shared tests remain in the tree. Implementer may import across packages freely.

- item: Map the apps/web ground truth the posting form must fit — how App.vue is structured today, how it calls the API and handles errors, which Naive UI components and form patterns the existing Chinese widgets already use, and where a posting/edit form naturally attaches. Independent of the HTTP shape the server tests are still pinning, so it can run now.
  status: done
  dispatched: ground-truth agent (already loaded with this codebase's conventions, idle after item 1); read-only over the worktree; output to land at kaola-workflow/issue-7/.cache/web-ground-truth.md
  result: kaola-workflow/issue-7/.cache/web-ground-truth.md (7 topics + Q1-Q10). App.vue is one 380-line monolith, no router/store/composables/api-module/components-dir, no <style> block (inline style attrs only). State is flat module-scope refs in per-feature clusters: xCreating/xMessage/xOk plus a list ref; three views off one `view` computed; features are n-divider blocks in one 工作台 n-card gated by canApprove (active+full) / canManageKeys (active). main.ts does app.use(naive) so every n-* is globally registered — n-form/n-form-item need no import change. NO fetch wrapper and NO useMessage/useNotification (no message provider in the tree). Eight bare fetch calls, all with credentials:'include' AND Accept:'application/json' — that Accept header is LOAD-BEARING: without it the server 302s to /login instead of returning 401 JSON. createProfile is quoted end-to-end as the template for createTask. NO form-validation idiom exists at all (zero n-form/n-form-item/rules; createProfile posts empty strings and lets the server 400). Profile dropdown is directly reusable with no new fetch: `profiles` already loads under the same canApprove gate, ProfileRow = {id:number, forge, base_url, repo_full_name, scopes_checked:unknown[], created_by:number}, response {profiles:[...]} — two cautions, scopes_checked is always [] (server writes literal '[]' and never updates) and the list is team-wide, unfiltered, no ORDER BY. The two Chinese errors are cleanly distinguishable at the source: insufficient perms RESOLVES with {missing:[...]} whose values are ALREADY the Chinese labels 读/推/PR (no client mapping table needed), unreachable REJECTS; client precedent for branching on body.error exists in createProfile's vault_unconfigured case. Also established: pr_convention and id are NOT form fields (DESIGN line 143 — prefixes derive from a platform-generated id), nor are poster/status/created_at; all 15 §6 fields tabulated to widget.
  orchestrator rulings taken from this report: (a) ADOPT n-form/n-form-item for the posting form despite no precedent — ~12 fields plus the credential XOR strain the flat-ref idiom, and the 权限不足 error wants per-field placement which only n-form-item gives; no new dependency since naive is globally registered. (b) KEEP the existing xCreating/xMessage/xOk flat-ref idiom for submit-level state rather than introducing useMessage, which would require adding a message provider to the tree — reuse before adding. (c) Web work is SEQUENCED AFTER the server contract is pinned, not parallel with it: the report grepped /api/v1/tasks and found three hits, all in docs and archived research, so a parallel web agent would code against invented key names.
  escalated: apps/web has NO test tooling of any kind (no test script, no vitest, no @vue/test-utils, no jsdom/happy-dom, no playwright/cypress; root pnpm test names seven server/package files only), so tdd-guide cannot author behavioral tests for the form and issue #7's acceptance criterion 凭证档案下拉选择 + 单任务临时 token 两种路径均可用 would rest on manual inspection. Adding tooling is a new dependency = escalation. Put to the user.

- item: Stand up web test tooling in apps/web — vitest + @vue/test-utils + happy-dom as devDeps, a `test` script, and wiring into the root pnpm test so the web suite actually runs. USER DECISION, taken in conversation 2026-08-21: approved as a new dependency, on the reasoning that CLAUDE.md forbids declaring done on weak signals and the form otherwise has no oracle at all. Must run when the worktree is free of other agents, since it needs pnpm install and that rewrites node_modules under anyone mid-run. Verify by making the new suite actually execute from the repo root, not just from apps/web.
  status: done
  dispatched: merged into the `tests-web` tdd-guide dispatch below — test infrastructure and test artifacts are both that role's custody, so splitting them across two agents would have handed the tooling to someone who cannot write the test that proves it works.
  result: Tooling landed in the worktree. apps/web gained vitest ^4.1.11, @vue/test-utils ^2.4.11, happy-dom ^20.11.6; `"test": "vitest run"`; vite.config.ts now imports from `vitest/config` with happy-dom + `src/**/*.test.ts`. Root package.json test script keeps the 8-file node --test invocation and appends `&& pnpm --filter @kaola/web test`. Orchestrator-verified files exist; App.vue untouched. Baseline: server/package 233/233 still pass, web 27 failed / 0 pass. Proof: kaola-workflow/issue-7/.cache/tests-web.md §1–2 and tests-web-baseline.txt.

- item: Author the posting-form tests in custody separate from the form itself, once the tooling exists and the server contract is pinned — both credential paths (profile dropdown and single-task inline token), the DESIGN §6 fields that are genuinely form fields, and the two distinct Chinese errors. Facts from the web map: the adapter's `missing` values are ALREADY the Chinese labels 读/推/PR so no client mapping table is needed; insufficient perms RESOLVES with {missing:[...]} while unreachable REJECTS; client precedent for branching on body.error is createProfile's vault_unconfigured case. NOT form fields: id, pr_convention, poster, status, created_at.
  status: done
  dispatched: tdd-guide agent `tests-web`, worktree .kw/worktrees/issue-7 — owns BOTH the vitest/@vue/test-utils/happy-dom tooling setup and the form test suite, writing apps/web test config + tests and wiring root pnpm test. Must NOT break the currently-green 233/233. Handoff, including the data-testid contract the form implementer must honour, to land at kaola-workflow/issue-7/.cache/tests-web.md
  result: apps/web/src/App.form.test.ts (652 lines, 27 tests, 0 pass at baseline). Load-bearing seam is the data-testid table in tests-web.md §3. Judgement 17 scopes the web half to create only — no board/edit required. Handoff: kaola-workflow/issue-7/.cache/tests-web.md. Baseline capture: .cache/tests-web-baseline.txt.

- item: Build the Chinese posting form in apps/web against the pinned server contract and the form test suite — credential-profile dropdown plus the single-task inline-token path, the §6 form fields, and the Chinese error surface distinguishing 权限不足 (naming which of 读/推/PR are missing) from 不可达. Create only (tests-web judgement 17). Orchestrator rulings already taken: adopt n-form/n-form-item (no new dep — naive is globally registered via app.use(naive) in main.ts); keep the existing xCreating/xMessage/xOk flat-ref idiom for submit-level state. Reuse the already-loaded `profiles` ref for the dropdown — no new fetch. Every fetch MUST carry credentials:'include' AND Accept:'application/json'. createProfile in App.vue is the quoted end-to-end template. Honour the data-testid contract and all 18 judgement calls in tests-web.md.
  status: done
  dispatched: implementer agent `impl-web`, worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7, writing apps/web/src/App.vue only; green proof to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/impl-web.md
  result: GREEN, orchestrator-verified — CI=true pnpm --filter @kaola/web test → 27 passed / 27. Custody held: git diff --stat shows only App.vue from this item. Form gated by canApprove; n-form/n-form-item; credential XOR via v-if; empty repo.base_branch/suggested_dir omitted; 422 at task-credential-feedback, everything else at task-message. Proof: kaola-workflow/issue-7/.cache/impl-web.md

- item: Author failing tests that pin the security-review repair — F1 profile-to-repo binding before decrypt, F2 http/https scheme+host on repo.base_url, F3 token-揭示 audit when a stored profile is decrypted for 发布即校验. Orchestrator rulings (reversing tests-server judgement 14): DESIGN §7 already scopes a profile by forge+仓库, so mismatch is a 400 before decrypt and no outbound request may carry the victim token. F2 is scheme+host only — do NOT block RFC1918, this product's self-hosted forges live there. F3 reuses DESIGN §10 type `token 揭示`; details must not contain the token. Append to apps/server/src/tasks.test.ts only; existing 59 must stay green.
  status: done
  dispatched: tdd-guide agent `tests-sec`, worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7, writing apps/server/src/tasks.test.ts only; handoff to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/tests-sec.md
  result: RED as required, orchestrator-verified — CI=true node --experimental-strip-types --test apps/server/src/tasks.test.ts → tests 69 / pass 59 / fail 10. The 10 failures are exactly the new F1/F2/F3 tests (201!==400 or 0!==1 token 揭示). Original 59 still pass. Pinned copy: '所选凭证档案与仓库不匹配。' / '仓库地址不是合法的 http 或 https 地址。' F3 details {profile_id, forge, base_url, full_name, outcome: ok|token_check_failed|forge_unreachable}. Handoff: kaola-workflow/issue-7/.cache/tests-sec.md. Baseline: .cache/tests-sec-baseline.txt

- item: Implement the F1/F2/F3 repair in apps/server/src/tasks.ts against the tests-sec suite — bind credential.profile_id to the profile row's forge/base_url/repo_full_name before decryptToken; reject non-http(s) repo.base_url before any adapter call; write a `token 揭示` event when a profile token is actually decrypted. Do not touch the test file. Do not invent a private-IP allowlist. Pinned Chinese and details JSON are in tests-sec.md §2.
  status: done
  dispatched: implementer agent `impl-sec`, worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7, writing apps/server/src/tasks.ts only; green proof to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/impl-sec.md
  result: GREEN, orchestrator-verified — CI=true node --experimental-strip-types --test apps/server/src/tasks.test.ts → 69 pass / 0 fail. Spot-checked source: isHttpOrHttpsUrlWithHost (new URL + http(s) + hostname) before adapter; exact === forge/baseUrl/fullName vs profile before decryptToken; insertTokenRevealEvent on 201/422/502 profile path only. Web still 27/27. Proof: kaola-workflow/issue-7/.cache/impl-sec.md

- item: Re-review the F1/F2/F3 repair delta in tasks.ts — confirm the original CRITICAL exfil is closed, that the RFC1918-not-blocked decision is residual (product hosts internal forges) rather than a regression, and hunt for new holes the repair introduced. Read-only. Same reviewer role as the first review, not the implementer.
  status: done
  dispatched: security-reviewer agent `sec-rereview`, read-only over the worktree; findings to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/sec-rereview.md
  result: VERDICT PASS, findings_blocking 0. Report: kaola-workflow/issue-7/.cache/sec-rereview.md. Live PoC: original F1 (gitlab profile + gitea + attacker base_url) is 400 mismatch and attacker received 0 victim plaintext. GitHub-forge lie, trailing slash, encoding, both-keys, inline-cannot-steal-stored all fail closed. F3 details dump contained neither plaintext nor ciphertext. Residual: inline-token path can still SSRF the caller's own token at any http(s) host — accepted for #7.

- item: Validate the whole tree in-session — run pnpm install inside the worktree first (it has no node_modules), then CI=true pnpm lint && typecheck && test && build must all exit 0. Route type/lint failures to build-error-resolver and behavior/coverage failures back to the test author.
  status: done
  dispatched: self, worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7; record to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/final-validation.md. Pre-docs gates already exit 0 (lint/typecheck/test/build); will re-run after documentation docks so the hash binds the tree that finalize will sink.
  result: ALL FOUR GATES GREEN after docs docked. Combined `CI=true pnpm lint && CI=true pnpm typecheck && CI=true pnpm test && CI=true pnpm build` exit 0. node --test 243/243; vitest 27/27. Hash `b8a6480294eb5fac131e912839a41004931b08eaa5638a0b46fc537024057d50` binds worktree `/Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7`. Record: kaola-workflow/issue-7/.cache/final-validation.md

- item: Measure whether the dev setup can authenticate the form at all — the web map reasons from source (unverified) that PUBLIC_URL defaults to localhost:3000 and OAuth callbacks are built from it, so a round trip from the Vite origin may land the session cookie on :3000 while the SPA runs on :5173 and proxies /api, leaving the form permanently unauthenticated in dev. If real, the fix is pointing PUBLIC_URL at the Vite origin. This is a fact to measure, not a judgement call — settle it by running it before anyone hand-verifies the form.
  status: done
  dispatched: investigator agent `dev-auth-probe`, worktree .kw/worktrees/issue-7 — must actually RUN the server and Vite rather than reason from source, since reasoning from source is exactly what left this unverified. Measurement to land at kaola-workflow/issue-7/.cache/dev-auth-probe.md
  result: MEASURED, verdict PARTIALLY TRUE — the premise was right and the stated consequence was WRONG. Report: kaola-workflow/issue-7/.cache/dev-auth-probe.md. The agent went idle without reporting; the work was recovered by checking the locator, not the worker. (a) The cookie half is FALSE: the session cookie is host-only for `localhost` with no Domain and no port binding, and cookies partition by host, never by port (RFC 6265 §8.5). Measured end-to-end — a session established at the :3000 callback returns 200 from http://localhost:5173/api/v1/me through the proxy. The UI is NOT left unauthenticated and there is nothing to fix. (b) The redirect half is TRUE and unconditional: completeOAuthLogin ends with `reply.redirect('/')` at apps/server/src/auth.ts:238, a relative redirect resolving against PUBLIC_URL, so every dev login lands on the Fastify placeholder 考拉任务服务占位 rather than the SPA. Session valid, so this is navigation/usability, not auth. (c) A third failure mode nobody claimed is REAL but conditional: opening the SPA at 127.0.0.1:5173 instead of localhost:5173 DOES break auth, because those are different cookie hosts — a host mismatch, not a port mismatch. Vite's console prints the localhost form, so the default path avoids it.
  does NOT block hand-verification of the posting form — the session is genuinely valid; the developer navigates back to :5173 manually.
  scope ruling (orchestrator): all three are pre-existing defects of the OAuth work from #3/#6, surfaced now only because #7 adds the first UI that needs an authenticated session to be useful. NOT fixed inside #7 — fixing auth here would be scope creep into another issue's surface. To be documented in the dev docs during docking and carried to the user as a follow-up issue proposal.
  incidental find, orchestrator-verified in source: `scope` appears NOWHERE in apps/server/src/auth.ts, so @fastify/oauth2 receives no scope and serializes String(undefined) — every authorize URL carries the literal `scope=undefined`. Harmless against the probe's local fakes, but a real GitHub/GitLab/Gitea provider would receive a scope literally named `undefined`. Unmeasured against a real provider. This is a latent defect in shipped auth code and belongs in the follow-up proposal.
  honest limits the investigator recorded rather than papering over: not measured against a real provider (local fakes served the token exchange and userinfo), not measured in a real browser (curl's jar implements the RFC 6265 host matching at issue, but browser-specific behavior was not exercised), and SameSite=Lax not stress-tested against a real cross-site provider redirect — expected to work by spec for a top-level GET return leg, but that is inference, not measurement.

- item: Security-review the new server surface before it is called done — apps/server/src/tasks.ts is 517 new lines that handle forge tokens, AES-256-GCM vault material, and permission gating, and DESIGN §7 makes token secrecy the core invariant of this product. The suite is green, but green is not reviewed and the tests can only assert what someone thought to assert. Read-only, so it runs concurrently with the web work.
  status: done
  dispatched: security-reviewer agent `sec-review` (NOT the implementer that wrote it), read-only over the worktree; findings to land at kaola-workflow/issue-7/.cache/sec-review.md
  result: VERDICT FAIL — three in-scope findings, one CRITICAL, and the review paid for itself. Agent went idle without reporting; recovered by checking the locator. Report: kaola-workflow/issue-7/.cache/sec-review.md
  F1 CRITICAL, orchestrator-verified in source: any authenticated active+full user can exfiltrate ANY stored credential profile's PLAINTEXT token to an arbitrary host. Reproduced end to end by the reviewer with `VICTIM PLAINTEXT TOKEN RECEIVED BY ATTACKER HOST: true`, and the request even returns 201 so the attack looks like an ordinary successful post. Mechanism, confirmed by reading the code myself: readRepo (tasks.ts:126) validates base_url only as a non-empty string — no scheme, host or allowlist check; readCredential (tasks.ts:168-180) never cross-checks the profile against the repo; tasks.ts:402 then decrypts the victim's token and tasks.ts:422 builds the adapter on the ATTACKER-supplied base_url, so validateToken sends the plaintext token to attacker.example. Nothing binds credential.profile_id to the task's repo, even though the profile row carries its own forge/base_url/repo_full_name and DESIGN §7 defines a profile as scoped 按「forge + 仓库」. Declaring forge:'gitea' is what makes base_url load-bearing — apiUrl pins GitHub to api.github.com, so a stored GitHub PAT is exfiltrated the same way simply by lying about the forge. Attacker population is wide: auth.ts:111-127 auto-provisions EVERY GitLab/Gitea login as active+full with no approval step. Blast radius: every token in credential_profiles, one HTTP request each; recovery means revoking every stored token at every forge.
  F2 MEDIUM: unvalidated repo.base_url gives an authenticated user an arbitrary server-side GET and a reachable-vs-closed oracle for internal hosts. Blind — no response body reflected.
  F3 MEDIUM: a vault token is decrypted and sent off-platform during 发布即校验 with NO audit event, contradicting DESIGN §7's 全量审计 requirement and CLAUDE.md's convention that reveals are always audited. Reproduced: after the F1 attack the events table holds only the victim's own profile-creation row. Not independently exploitable — it is the detection gap that makes F1 silent and un-forensicable.
  clean and explicitly NOT suppressed, each checked by injection or .toSQL() rather than by eye: no token reaches any response, error path, or log (Fastify built with no logger; the catch at tasks.ts:429 swallows the error object); AES-256-GCM use is correct (fresh 12-byte IV per encryption, explicit authTagLength 16, tag verified before final(), tampered blobs fail authentication rather than yielding plaintext); permission gating matches DESIGN §11 with no check-after-use and no route trusting a client-supplied actor; the credential XOR cannot be bypassed (probed {}, both-keys, null, empty-string, and __proto__ — Fastify's secure-json-parse strips __proto__ before the reader sees it); no SQL injection (tasks.ts:322 compiles to a bound-parameter form, verified with .toSQL()); no state-distinguishing oracle in profile lookup.
  ORCHESTRATOR RULING — reverses tests-server judgement call 14, which said task-repo-vs-profile-repo mismatch was unspecified and told the implementer not to add a rule the suite does not pin. That call was wrong, and the security review is the evidence: the mismatch rule IS the security boundary. Binding the profile to the repo is not inventing policy — DESIGN §7 already defines a profile as scoped by forge + repository, so this enforces the stated design rather than extending it.
  out-of-scope, recorded so they are not lost: auth.ts:111-127 auto-active+full for every GitLab/Gitea login and any full member being able to approve anyone (pre-existing, #5, defines F1's attacker population); auth.ts:258 session cookie secure:false (pre-existing, #5, a TLS/deployment decision); source.issue_url and description_md stored verbatim with no rendering sink today but a live XSS/prompt-injection concern when the board UI lands in #8; deleting a credential profile leaves a dangling credential_profile_id with no FK, which fails closed and belongs to #9.

- item: Dock documentation against what actually shipped — README, CHANGELOG, CLAUDE.md Commands and snapshot, docs/api.md, docs/architecture.md, transcribing real signatures and real command output. MUST correct the standing CLAUDE.md error that `@kaola/forge-adapters` exports `validateToken`: it does not, only `createForgeAdapter` is exported and validateToken is reachable solely as an adapter method. DESIGN.md §6 was already changed deliberately in an earlier item and is not to be touched again here.
  status: done
  dispatched: doc-updater agent `docs`, worktree /Users/ylpromax5/Workspace/KaolaTasks/.kw/worktrees/issue-7; report to land at /Users/ylpromax5/Workspace/KaolaTasks/kaola-workflow/issue-7/.cache/doc-updater.md
  result: DOCKED. Worktree README / CHANGELOG Unreleased (#7 at top) / CLAUDE.md snapshot+Commands / docs/api.md / docs/architecture.md. CLAUDE.md now says createForgeAdapter is the export and validateToken is a method; task HTTP implemented; MCP and claim not; server workspace:* deps. DESIGN.md untouched. Proof: kaola-workflow/issue-7/.cache/doc-updater.md
