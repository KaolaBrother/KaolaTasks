# Project Instructions

## Project Snapshot

- Purpose: 考拉任务 (Kaola Tasks) — an internal Chinese-language task board where team members post/import coding tasks (GitHub / GitLab / Gitea issues) with a forge token attached, and teammates' agents claim them via MCP, implement on the real repo, and deliver a PR. Platform is routing/coordination only — no agent execution, no code hosting.
- Stack: TypeScript full-stack — Vue 3 + Vite + Naive UI (web), Node 22 + Fastify (API), Drizzle ORM + SQLite, official MCP TypeScript SDK, pnpm workspaces monorepo.
- Architecture: `apps/web` (前端) + `apps/server` (API + MCP server + webhooks) + `packages/shared` (task-brief zod schema, state machine) + `packages/forge-adapters` (GitHub/GitLab/Gitea behind one interface). Full design: `docs/DESIGN.md`. M0 shells remain (`getSharedHealth` → `kaola-shared-ready`, `getForgeAdaptersHealth` → `kaola-forge-adapters-ready`); naked `buildApp()` (omit/empty `webDist` and `viteDevTarget`) `GET /` is `text/plain; charset=utf-8` body `考拉任务服务占位` via `getPlaceholderBody()`; non-empty `webDist` serves SPA (`@fastify/static`); only `viteDevTarget` proxies Vite (`@fastify/http-proxy`); both set → `webDist` wins. `@kaola/shared` implements the Task Brief zod schema (`taskBriefSchema` / `parseTaskBrief`; `credential` is `z.union` of `{ profile_id: z.string() }` and `{ inline: z.literal(true) }`) and `transitionTaskStatus`; `@kaola/forge-adapters` exports `createForgeAdapter(kind, options?: { baseUrl?, webhookSecret? })` (`validateToken`, `getPullRequest`, `importIssue`, `listIssues`, `registerWebhook`, `parseWebhook`, `commentOnIssue` are adapter methods, not package-level exports; package-level `parseIssueUrl(kind, issueUrl)` returns `{ full_name: string } | undefined`; package-level `WebhookSignatureError extends Error`; `ImportedIssue` is `{ title, description_md, issue_url, repo: { full_name } }`; `ListedIssue` is `{ number, title, issue_url }` (`listIssues`); `ForgeEvent` is `{ type: 'pull_request'; state: 'merged' | 'closed'; pr_url: string; repo: { full_name: string } }`; `IssueRef` is `{ issue_url: string }` (no longer `unknown`, #14); `commentOnIssue(cred, issueRef, body)` posts a comment (GitHub/Gitea) or note (GitLab) to the issue and is implemented (#14, was `notImplemented`)); `registerWebhook`/`parseWebhook` implemented for GitHub (`X-Hub-Signature-256` HMAC-SHA256, `sha256=` prefix), GitLab (`X-Gitlab-Token` plaintext, timing-safe compare — legacy secret-token scheme, not `signing_token`), Gitea (`X-Gitea-Signature` HMAC-SHA256, no prefix); missing/empty `webhookSecret` throws `WebhookSignatureError`; signature-valid-but-irrelevant deliveries (ping, wrong event type, non-`closed` action) return `null`, never throw. `@kaola/server` depends on `@kaola/shared` and `@kaola/forge-adapters` (`workspace:*`) and has local-setup + GitLab/Gitea OAuth + `users` (`GET /login` wizard-or-login HTML, `GET/POST /api/v1/setup`, `POST /api/v1/login`, `/login/{gitlab|gitea}` (+callback), `GET /login/github` and callback 404, `GET /api/v1/me` with `permission_level` `admin`|`full`|leftover `claim_only`, `GET /api/v1/users`, `POST /api/v1/users/:id/promote`; `POST /api/v1/users/:id/approve` retired 404; `KAOLA_ADMINS` ignored); Agent API keys (`POST/GET /api/v1/agent-keys`, `DELETE /api/v1/agent-keys/:id`, Bearer `GET /api/v1/agent/whoami`); credential profiles + AES-256-GCM vault (`GET/POST /api/v1/credential-profiles`, `DELETE /api/v1/credential-profiles/:id`, `GET /api/v1/credential-profiles/:id/issues` #19 — session `canPublish` (`active`+`admin` or `active`+`full`), decrypt then `listIssues`, `502` message `无法连接 forge 列出 Issue。`, does not write `token 揭示`, not a third reveal channel; `revealCredentialProfile` is a module export, not itself HTTP); task HTTP (`registerTasks`: `GET/POST /api/v1/tasks`, `POST /api/v1/tasks/import` 200 draft — no persist, no `validateToken`, never a token — `GET/PATCH /api/v1/tasks/:publicId`; `tasks` table); claim HTTP (`registerClaim`: Bearer `POST /api/v1/tasks/:publicId/claim|progress|release`; extracted `claimTask`/`reportProgress`/`releaseTask`/`submitPr` (`claimTask`/`submitPr` now `async`, each awaiting a write-back attempt after their own status transition); claim `201` / MCP `claim_task` success `clone` four keys `suggested_dir`/`token_usage`/`remote_url`/`extra_header` (gitea Authorization `token ${token}`, else `Bearer ${token}`; `remote_url` strips trailing slashes on `base_url` then `/{full_name}.git`); `202` omits clone; `leases` table; TTL `86400`; Bearer hook `addAgentBearerHook` in `agent-bearer.ts`, also used by whoami and MCP). MCP (`registerMcp`: Bearer `POST /api/mcp` Streamable HTTP, GET/DELETE `405`; six tools `list_tasks`/`get_task_brief`/`claim_task`/`report_progress`/`release_task`/`submit_pr`; `@modelcontextprotocol/sdk` `1.30.0`; `zod` `^4.4.3`; `submissions` table; no REST `submit_pr`). PR polling + webhook receiver (both drive `待验收`→`已完成`/`已退回` through the same extracted `applyPrTerminalTransition(db, task, submissionId, terminal, prUrl)` in `poller.ts`): `pollPendingReviews(db, forgeInstances?)` — `@kaola/forge-adapters` `getPullRequest` → `{ state: 'open'|'merged'|'closed' }`; skips (zero fetches) any task whose `(repoForge, repoBaseUrl)` matches a `syncMode: 'webhook'` entry in `forgeInstances` (`taskMatchesForgeInstance`); omitted/`[]` polls every `待验收` row as before #13. `POST /api/v1/webhooks/:publicId` (`registerWebhooks` in `webhook.ts`) — no session, no Bearer, forge signature is the sole auth; `:publicId` is a `forgeInstances[]` entry id, not a task id; `404` `{ error: 'not_found' }` unknown instance; `401` `{ error: 'invalid_signature' }` on `WebhookSignatureError`; else `204` (ping/irrelevant/no-match/already-final all included). Match is bound to the signature-verified instance's `(forge, baseUrl)` before comparing `pr_url` (closes a cross-instance confused-deputy gap found and fixed in review). `buildApp({ pollIntervalMs?, forgeInstances? })`: `pollIntervalMs` omit/`<=0` = no timer, positive = `setInterval` + `onClose` clear; `forgeInstances` threads into both the poller timer and `registerWebhooks`; no new database table (config only). Prod `index.ts` reads `POLL_INTERVAL_MS` (empty/unset → `60000`) and `FORGE_INSTANCES` (JSON array; empty/unset → `[]`; invalid JSON fails boot, never silently poll-everything). `状态迁移` events from either path use `actorUserId: null`. New `apps/server/src/writeback.ts` (#14): `attemptWriteback(db, task, transition, actorUserId, prUrl?)` posts a `commentOnIssue` status comment for **imported** tasks only (`sourceType === 'imported'` + non-empty `sourceIssueUrl`; native tasks are a no-op) on transitions `'认领' | '提交PR' | '完成'` (exact strings) — hooked after commit inside `claimTask` (认领), `submitPr` (提交PR), and `applyPrTerminalTransition` **only when `terminal === 'merged'`** (完成; never on `已退回`/`closed`, never on `releaseTask`); every failure (forge throw, decrypt miss, unparseable URL) is swallowed so the caller's own success is unaffected. On success writes `events.type` `回写`, `details` **exactly** `{ task_id, transition, ok: true, issue_url }` (`task_id` is the public id; no token). Comment body always contains `task.publicId` and `PUBLIC_URL` (trailing slash trimmed, default `http://localhost:31415`); 提交PR/完成 bodies also include `pr_url`. Credential is the task's own (profile or inline) via `decryptTaskToken` (moved from the poller into `writeback.ts`; the poller still uses it for `getPullRequest`) — never the caller's Agent API key. `retryPendingWritebacks(db): Promise<void>` (exported from `writeback.ts`, re-exported from `poller.ts`; never rejects) re-attempts any imported task whose transition has occurred but has no successful `回写` yet; the `app.ts` timer runs it every tick right after `pollPendingReviews`, under the same in-flight guard. On a **merged** webhook delivery, write-back now decrypts the task's forge credential to post the 完成 comment — the merge/close verdict itself is still decided from the signed payload alone (`parseWebhook`, no `getPullRequest` on that path); `closed`/已退回 deliveries still never write back and still never decrypt. Forge token reveal: REST claim `201` top-level `token` and MCP `claim_task` success `token` remain the only two HTTP reveal channels; session GET list/get, `POST /api/v1/tasks/import` `200`, and `GET /api/v1/credential-profiles/:id/issues` never contain it; write-back (including the webhook-merged decrypt) never puts a token, secret, or ciphertext in a response, log, or `events.details`. `insertAuditEvent` accepts `actorUserId: number | null` (also accepts a `db.transaction` handle via a structural `AuditEventWriter` type). Audit log + team stats (#15): `registerEvents` (`apps/server/src/events.ts`) adds `GET /api/v1/events` → `{ events: [{ id, type, actor_user_id, actor_username, created_at, details }] }` newest-first (`orderBy(desc(events.id))`, `actor_username` via left join on `users`, `details` JSON-parsed) and `GET /api/v1/stats` → exactly `{ completed_count, completed_by_username }` (counts `events.type === '状态迁移'` rows whose `details.to === '已完成'`; grouped by `actor_username`, null actor under `'系统'`). Both are session-gated stricter than the task board: `user.status !== '待批准'` (`canReadEvents`), so a pending user is `401` on both (same oracle as `GET /api/v1/me`) even though it can list tasks. No query string on either route; all filtering (类型/人/任务/时间 for events) is client-side in `@kaola/web`. Claim confirmation for autonomous agents (#16): `claimTask` (`claim.ts`) gains an optional 4th parameter `autonomous?: boolean` (REST body `{ autonomous?: boolean }`, MCP `claim_task` input gains optional `autonomous`; missing/non-boolean body = instructed, unchanged 认领即授权 `201`). When `autonomous === true` and the claiming user's `trustedAutomation` is not `true`: an existing `'approved'` `claim_confirmations` row for that `(task, user, agent_key)` triple is consumed (deleted, one-time use) and the claim proceeds to the normal `201`; otherwise a `'pending'` row is inserted/reused, `events.type` `认领待确认` is written, and the response is `202` `{ error: 'confirmation_required', message, pending: true }` — no token, no lease, task stays `待认领` (the pre-existing `待批准` `403` gate still runs first). `registerClaimConfirmations` (`claim-confirmations.ts`) adds session-only (`requireActiveSessionUser` then `canManageInstance` admin-only; 401/待批准 then 403 for publisher) `GET /api/v1/claim-confirmations` → `{ confirmations: [{ id, task_id, state, created_at }] }` scoped to the caller's own rows, and `POST /api/v1/claim-confirmations/:id/approve` / `.../reject` → `200 { ok: true }` (writes `认领已确认` on approve; non-owned/missing id on either → `404`); approve/reject themselves never insert a lease, flip task status, or reveal a token. `auth.ts`'s `GET /api/v1/me` gains additive `trusted_automation: boolean` (default `false`); new `PUT /api/v1/me/settings` body `{ trusted_automation: boolean }` → `200 { trusted_automation }` (session/待批准 then admin-only `canManageInstance`; publisher 403; non-boolean body → `400`). `schema.ts`/`db.ts`: `users.password_hash` (nullable), `provider` includes `local`, `permission_level` includes `admin`, unique index `users_local_username`, `promoteEarliestLoginableAdmin`; `users.trusted_automation` (boolean, default `false`, added via `ALTER TABLE` with a duplicate-column swallow); `claim_confirmations` (`id`, `task_id`, `user_id`, `device_id`, nullable leftover `agent_key_id`, `state` `'pending'|'approved'|'rejected'`, `created_at`). `app.ts` wires `registerClaimConfirmations` then `registerEvents`, both after `registerClaim` and before `registerMcp`. Forge token reveal is still exactly the same two channels; claim `202`, `GET /api/v1/events`, `GET /api/v1/stats`, `GET/POST /api/v1/claim-confirmations*`, `GET /api/v1/me`, and `PUT /api/v1/me/settings` never contain one. `@kaola/web` member workbench (`view === 'member'`) is a four-pane shell (nav 看板/发布/钥匙/审计; panes `v-show`; Naive `theme-overrides` from `theme.ts` plus `theme.css`; no vue-router) with Chinese 任务看板 in the 看板 pane (列表/看板, client-side filters, detail + one synthetic 发布 from `created_at`+`poster`; imported detail shows 导入内容; `GET /api/v1/tasks` with no query; poster `canPublish` 取消/重新开放 via existing `PATCH /api/v1/tasks/:publicId`), 发布 pane (`canPublish` = `active`+`admin` or `active`+`full`; form groups credential→repo→task + `<details>` 高级; no `task-group-acceptance`; imported `task-import-card` only after import 200; `POST /api/v1/tasks` JSON `{ title, description_md, source, repo, credential }` omitting extra keys; leftover `claim_only` omits 发布 and 凭证档案), 电脑 pane (受信自动化 toggle `PUT /api/v1/me/settings` + 待确认认领 `GET/POST /api/v1/claim-confirmations*` + 待授权电脑 when `canManageInstance` = `active`+`admin`; publishers cannot bind devices), 审计 pane (client-side 类型/人/任务/时间 filters over `GET /api/v1/events` + 团队统计 `GET /api/v1/stats`); still no Agent claim UI (humans confirm/reject in the web UI, Agents claim via Bearer/MCP); no webhook/forge-instance UI. 发布任务 form 来源 `imported` shows 导入 button then `POST /api/v1/tasks/import`; import 200 shows `task-import-card` (`task-import-source-label` 导入内容 on the card only); profile path uses Issue/档案 dropdowns (empty hint `暂无凭证档案，请先到钥匙页添加。`, hides Forge/仓库地址/仓库) and `GET /api/v1/credential-profiles/:id/issues`; inline token still pastes Issue URL. Tests: `App.shell.test.ts` (11 `it(`) via vitest `include` `src/**/*.test.ts`. `VAULT_MASTER_KEY` (64 hex) is required at encrypt/decrypt, not at `buildApp()` boot.
- UI language is Chinese (中文界面); code identifiers and docs comments in English.

## Commands

- Install: `pnpm install` (`packageManager` `pnpm@11.19.0`; `engines.node` `>=22`)
- Test: `pnpm test` → `node --experimental-strip-types --test packages/shared/src/index.test.ts packages/forge-adapters/src/index.test.ts packages/forge-adapters/src/validate-token.shared.test.ts packages/forge-adapters/src/get-pull-request.shared.test.ts packages/forge-adapters/src/import-issue.shared.test.ts packages/forge-adapters/src/list-issues.shared.test.ts packages/forge-adapters/src/webhook.shared.test.ts packages/forge-adapters/src/comment-on-issue.shared.test.ts apps/server/src/import.test.ts apps/server/src/placeholder.test.ts apps/server/src/password.test.ts apps/server/src/auth.test.ts apps/server/src/auth-cookie.test.ts apps/server/src/identity.test.ts apps/server/src/agent-keys.test.ts apps/server/src/vault.test.ts apps/server/src/credential-profile-issues.test.ts apps/server/src/tasks.test.ts apps/server/src/hosting.test.ts apps/server/src/claim.test.ts apps/server/src/mcp.test.ts apps/server/src/poller.test.ts apps/server/src/webhook.test.ts apps/server/src/writeback.test.ts apps/server/src/events.test.ts apps/server/src/claim-confirm.test.ts apps/server/src/devices.test.ts apps/mcp/src/main.test.ts && pnpm --filter @kaola/web test` (root `package.json` file list; `apps/web`'s own `App.audit.test.ts` / `App.settings.test.ts` / `App.shell.test.ts` run inside that `vitest` invocation via its `include: 'src/**/*.test.ts'`, not listed individually here)
- Lint: `pnpm lint` → `eslint .`
- Typecheck: `pnpm typecheck` → `pnpm -r --if-present typecheck`
- Build: `pnpm build` → `pnpm -r --if-present build`
- Dev server: root `pnpm dev` (`node scripts/dev.mjs`; Fastify `PORT` default `31415` + Vite `127.0.0.1:5173 --strictPort` with `VITE_DEV_TARGET` default `http://127.0.0.1:5173`; advertised origin is 31415). `pnpm --filter @kaola/server start` (`node --experimental-strip-types src/index.ts`; `HOST` default `0.0.0.0`, `PORT` default `31415`; `SQLITE_PATH` default `:memory:`; passes `WEB_DIST`, `VITE_DEV_TARGET`, `pollIntervalMs` (from `POLL_INTERVAL_MS`, empty/unset → `60000`), and `forgeInstances` (from `FORGE_INSTANCES`, empty/unset → `[]`, invalid JSON fails boot) into `buildApp({ sqlitePath?, webDist?, viteDevTarget?, pollIntervalMs?, forgeInstances? })`). Naked `buildApp()` `GET /` body `考拉任务服务占位`; SPA when `webDist` is set; Vite proxy when only `viteDevTarget`; `pollIntervalMs` omitted/`<=0` registers no poller timer, a positive value registers one `setInterval` cleared on `app.close()`; `forgeInstances` (`Array<{ publicId, forge, baseUrl, syncMode: 'webhook'|'poll', webhookSecret }>`) feeds both the poller skip logic and `POST /api/v1/webhooks/:publicId`, no new table. `registerAuth` requires non-empty `SESSION_SECRET`, `OAUTH_GITHUB_CLIENT_ID`, `OAUTH_GITHUB_CLIENT_SECRET`, `OAUTH_GITLAB_CLIENT_ID`, `OAUTH_GITLAB_CLIENT_SECRET`, `OAUTH_GITLAB_BASE_URL`, `OAUTH_GITEA_CLIENT_ID`, `OAUTH_GITEA_CLIENT_SECRET`, `OAUTH_GITEA_BASE_URL`. Optional `PUBLIC_URL` default `http://localhost:31415`. `VAULT_MASTER_KEY` (64 hex chars) is read by `encryptToken`/`decryptToken`, not required at `buildApp()` boot. `pnpm --filter @kaola/server dev` (`node --watch --experimental-strip-types src/index.ts`); `pnpm --filter @kaola/web dev` (`vite`).
- Forge smoke streamer: `pnpm smoke:forge -- gitlab|gitea` (`scripts/forge-smoke.ts`) is the unattended end-to-end loop for **publish + claim** against a real GitLab or Gitea repo (not GitHub; `github` exits). It walks `ensureSetup` → stub GitLab publisher OAuth (`full`) → credential profile → import Issue → publish → pair device → `claim_task` → clone envelope → open PR → `submit_pr` → merge → `pollPendingReviews` → `已完成` + 回写. Playbook and log: `docs/smoke-test.md`. Claim identity is device proof + remote bind; the claimant needs no forge PAT. Isolated SQLite + stub GitLab OAuth; does not touch `pnpm dev`. Real env is only `GITLAB_TOKEN` / `GITEA_TOKEN`; missing `SESSION_SECRET` / `VAULT_MASTER_KEY` / nine `OAUTH_*` / `PUBLIC_URL` are filled in-process (`ensureSimulatedAuthEnv`, no `.env`, does not overwrite set values). Production `pnpm dev` still requires the operator to export those boot vars.

## Non-Negotiable Rules

- Think before coding: state assumptions, surface ambiguity, and ask when unclear.
- Read before writing: inspect the target file and relevant surrounding conventions immediately before editing or creating files.
- Keep it simple: solve the requested problem without speculative abstractions.
- Make surgical changes: touch only what the task requires.
- Goal-driven execution: Define verifiable success criteria before starting. Keep the tests in separate custody from the code they judge — whoever implements a behavior does not author its tests. Loop until criteria pass; don't declare done on weak signals.
- Verify facts, don't fabricate: do not guess API/library behavior, interfaces, or signatures — confirm them against documentation, source, or a run before relying on them. Do not claim to understand code, errors, or requirements you have not verified; name what you do not know and find out.
- Reuse before adding: before writing a new interface, search for an existing equivalent and extend it rather than duplicate functionality.
- Escalate irreversible changes: do not unilaterally make hard-to-reverse changes or alter a user-owned contract (public API, schema or data migration, dependency or build-tooling swap, deletion of working capability); state the decision and its evidence, then get confirmation before proceeding.

## Validation Policy

- Treat background hooks as advisory; do not duplicate validation they already perform.
- Own verdicts locally: run tests/lint/build in-session to judge done; CI is confirmation, not the judge.

## First Principles

The numbered axioms are tie-breakers, applied in priority order whenever a situation is not already settled; the paragraphs that follow them are standing defaults that hold whether or not anything else settles the case.

1. **Correct first.** Never trade correctness for speed or cost; rework is the most expensive outcome.
2. **Then save human time.** Remove manual steps and shorten the wait, without weakening axiom 1.
3. **Then spend as little as possible.** Use the cheapest sufficient mechanism — parallelism, extra agents, and higher model tiers are means, not goals.
4. **Machines decide facts; humans decide values.** Take irreversible and value-laden calls to the user and ask, in conversation; leave everything checkable to run automatically.
5. **Own your own verdicts.** Never let a system the workflow does not own (CI, an external service) be the judge of done.

**Tie-breaker protocol:** when nothing else covers a situation, resolve it by walking these axioms in order and record a one-line derivation alongside the work. Recording it is useful and never required.

**Check the premise before it shapes the work:** an issue is a claim recorded earlier against a tree that has since moved, so establish what is true *now* at the place it points and let the measurement rather than the filed text decide what gets built. The usual outcome is neither *right* nor *wrong* but right-with-a-detail-that-misroutes — a stale locator, a miscounted set, a clause that breaks if executed literally — so carry the measurement forward, never a bare verdict. Where the two disagree the issue gets corrected, not quietly worked around. Nothing inspects that you did this.

**Dispatch production; keep decisions:** the orchestrator's context is the run's scarcest resource — a handoff costs once, inline residue taxes every later decision — so delegating discretionary production is the default and only the deciding stays inline; weigh the economics per case by judgment, with no justifier, evidence line, or approval attached.

**Parallel by default:** concurrency is the standing default for independent work, and work that genuinely feeds other work runs in order because it has to. Nothing inspects that choice — no proof, no evidence line, no cap: you can tell the difference, and the frontier is in front of you. Width stays sized to the true shape of the task rather than pushed as wide as it will go.

## Kaola-Workflow

<!-- KW-AGENTS-MANAGED-START -->
Everything between these markers is owned by `workflow-init`; owner content outside them is preserved.

<!-- PIN: forge-is-the-backlog -->
- Start or resume workflow work through the router entrypoint installed by the active runtime.
- The forge is the backlog authority; freshly verify issue state before it shapes implementation.
- `kaola-workflow/.roadmap/_rules.md` is the one optional local file that survives for standing project rules.
- nothing else is generated or tracked under `kaola-workflow/.roadmap/`; there is no local mirror to refresh.
- Top-priority labels: declare in `kaola-workflow/config.json` (`priority_top_tier_labels`).
- A run records its claim in `kaola-workflow/{project}/workflow-state.md` and its missions in
  `kaola-workflow/{project}/mission-list.md`.
- Keep each mission as `item`, `status`, `dispatched`, and `result`.
- Each entry is a mission, not a specification.
- The frontier is the list minus done minus in-flight; choose dispatch or inline per item by execution economics.
- **Three write moments.** Create the mission, record `dispatched` **before the work goes out**, then record `result` when it closes.
- `dispatched` records what went out, to whom, and where the output was to land.
- Once closed, the completed item and its result are immutable.
- The invariant is one dispatch has one result; if later work appears, append a new mission.
- Name roles by function and reasoning tier, never by a vendor model name; write `planner (heavy-reasoning tier)`.
- Use the vendored agent role names exactly as installed; prefer short names like `planner`. Spawn the type
  this runtime's installed workflow-next / finalize instructions name for that role. Follow those instructions
  for whether the spawn call carries a model argument. Do not substitute a generic built-in type unless those
  same instructions explicitly map the role onto one.
- After resume or compaction, read the workflow state and mission list before continuing.
- Finalize only after focused and integration evidence pass, documentation is docked, and every finding closes.
- Archive completed run state through the installed workflow lifecycle rather than deleting it by hand.
<!-- KW-AGENTS-MANAGED-END -->

## Project Conventions

- `docs/DESIGN.md` is the product/architecture source of truth; change it before changing contracts (task-brief schema, state machine, adapter interface, MCP tool surface).
- The three forge adapters stay behavior-identical: one shared integration-test spec runs against all three implementations.
- Tokens are never logged, never returned by `list_tasks`/`get_task_brief` or session GET list/get; reveal via Bearer `POST /api/v1/tasks/:publicId/claim` `201` top-level `token` and MCP `claim_task` success `token` (audited) — those two remain the only HTTP that ever returns a forge token. `POST /api/v1/tasks/import` `200` and `GET /api/v1/credential-profiles/:id/issues` never contain a forge token (`GET …/issues` decrypts server-side like the poller and does not write `token 揭示`). `pollPendingReviews` decrypts a task's credential to call the forge adapter but never puts it on an HTTP response or in `events.details`. `POST /api/v1/webhooks/:publicId` itself is not a third reveal channel: its `401`/`204` responses never contain the `webhookSecret`, an expected digest, or a forge token — but on a **merged** delivery it now triggers `attemptWriteback` (#14), which does decrypt the task's forge credential (as the poller already does) to post a status comment via `commentOnIssue`; the token still never appears in that `204`, in logs, or in `events.details` (only `{ task_id, transition, ok, issue_url }`). `closed`/已退回 deliveries still never write back and still never decrypt.
- Task lifecycle states use the Chinese names from `docs/DESIGN.md` §5 (待认领/进行中/待验收/已完成/已退回/已取消) as the canonical enum labels.

## Documentation Map

- `README.md` — project overview and usage.
- `CHANGELOG.md` — user-visible changes.
- `docs/README.md` — documentation index.
- `docs/DESIGN.md` — full product & system design (v0.3, source of truth).
- `docs/architecture.md` — system structure and data flow.
- `docs/api.md` — APIs, schemas, events, and external contracts.
- `docs/conventions.md` — coding, testing, Git, and review rules.
- `docs/decisions/` — architecture decision records.
- `docs/smoke-test.md` — GitLab / Gitea smoke streamer playbook (full publish + claim loop) plus this run's log. Publish surface is not GitHub. **配合** browser steps wait for the human; unattended runs use `pnpm smoke:forge -- gitlab|gitea`. Do not write smoke progress into GitHub issues.

## Documentation Update Checklist

When install/lint/test/build commands, workspace members, or HTTP surface change: `README.md`, `CHANGELOG.md`, this Commands section in `AGENTS.md`, `docs/architecture.md`, `docs/api.md`. Do not change `docs/DESIGN.md` contracts as a side effect of scaffolding.

## Maintenance

- Keep this file under 200 lines — a recommendation, not a limit; move detail to docs or skills.
- Add rules only after repeated mistakes, review feedback, or stable project conventions.
- Do not use `@path` imports for optional reference material.
